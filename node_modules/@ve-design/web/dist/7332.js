function supportsPopover() {
    return "u" > typeof HTMLElement && 'function' == typeof HTMLElement.prototype.showPopover;
}
function showPanelPopover(panel) {
    if (!panel || !panel.isConnected || !supportsPopover()) return;
    if (panel.matches(':popover-open')) return;
    try {
        panel.showPopover();
    } catch  {}
}
function hidePanelPopover(panel) {
    if (!panel || !supportsPopover()) return;
    if (!panel.matches(':popover-open')) return;
    try {
        panel.hidePopover();
    } catch  {}
}
export { hidePanelPopover, showPanelPopover };
