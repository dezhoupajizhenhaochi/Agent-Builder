import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_THINKING_TAG = "ve-thinking";
export declare const VE_THINKING_TOGGLE_EVENT = "ve-thinking-toggle";
export interface ThinkingToggleDetail {
    expanded: boolean;
}
export interface VeThinkingProps {
    title?: string;
    loading?: boolean;
    expanded?: boolean;
    maxHeight?: string;
    autoScroll?: boolean;
}
/**
 * Thinking component for displaying public AI reasoning summaries in chat.
 *
 * @tag ve-thinking
 * @slot - Thinking body content. Streaming updates are owned by the consumer.
 * @slot icon - Custom icon, replaces the default icon for both loading and idle states.
 * @slot header-extra - Extra content placed at the end of the header row, before the caret.
 * @csspart header-row - The full-width row that wraps the header button and extra content.
 * @csspart header - The top title row and expand trigger.
 * @csspart header-extra - The slot wrapper for extra header content.
 * @csspart icon - The non-loading status icon.
 * @csspart indicator - The loading animated indicator.
 * @csspart label - The title label.
 * @csspart caret - The expand/collapse caret.
 * @csspart body - The body wrapper.
 * @csspart step-line - The vertical line beside the body content.
 * @csspart content - The body content and scroll container.
 * @fires ve-thinking-toggle - Fired when users toggle the thinking body.
 */
export declare class VeThinkingElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Title text rendered in the header.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    /**
     * Whether the thinking content is still streaming.
     *
     * @attr
     */
    accessor loading: boolean;
    /**
     * Whether the thinking body is expanded.
     *
     * @attr
     */
    accessor expanded: boolean;
    /**
     * Maximum body height. Overflow content scrolls inside the content area.
     *
     * @attr max-height
     */
    accessor maxHeight: string;
    /**
     * Whether streaming content should follow the bottom while the user is already near the bottom.
     *
     * Accepts `auto-scroll="false"` to disable in HTML.
     *
     * @attr auto-scroll
     */
    accessor autoScroll: boolean;
    private accessor scrollContainer;
    private accessor iconSlot;
    private accessor loadingCanvas;
    private accessor hasIconSlot;
    private accessor canScrollStart;
    private accessor canScrollEnd;
    private scrollResizeObserver;
    private contentMutationObserver;
    private scrollFrame;
    private blobFrame;
    private blobLastTime;
    private blobAnimationFrame;
    private shouldStickToBottom;
    private titleText;
    constructor();
    connectedCallback(): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    updated(_changedProperties: PropertyValues<this>): void;
    disconnectedCallback(): void;
    /**
     * Toggles the thinking body.
     */
    toggle(): void;
    private applyMaxHeightToken;
    private isNearBottom;
    private observeScrollableContent;
    private handleContentChange;
    private scheduleScrollStateUpdate;
    private updateScrollState;
    private handleScroll;
    private handleHeaderClick;
    private handleHeaderKeydown;
    private syncSlotState;
    private handleIconSlotChange;
    private syncLoadingBlobAnimation;
    private startLoadingBlobAnimation;
    private stopLoadingBlobAnimation;
    private tickLoadingBlob;
    private createLoadingBlobGradient;
    private drawLoadingBlobFrame;
    private renderThinkingIcon;
    private renderCaret;
    private renderHeaderLabel;
    private renderHeader;
    private renderBody;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-thinking': VeThinkingElement;
    }
    interface HTMLElementEventMap {
        've-thinking-toggle': CustomEvent<ThinkingToggleDetail>;
    }
}
