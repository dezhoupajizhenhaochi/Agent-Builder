export interface TabsPaneSnapshot {
    key: string;
    disabled: boolean;
}
export interface ResolveTabsStateInput {
    panes: readonly TabsPaneSnapshot[];
    activeTab: string | undefined;
    defaultActiveTab: string;
    innerActiveKey: string;
    focusKey: string;
}
export interface ResolveTabsStateResult {
    resolvedActiveKey: string;
    nextInnerActiveKey: string;
    nextFocusKey: string;
}
export declare function resolveTabsState({ panes, activeTab, defaultActiveTab, innerActiveKey, focusKey, }: ResolveTabsStateInput): ResolveTabsStateResult;
