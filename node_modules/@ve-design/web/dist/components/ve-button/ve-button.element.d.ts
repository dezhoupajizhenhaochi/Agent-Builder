import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_BUTTON_TAG = "ve-button";
export type ButtonType = 'primary' | 'secondary' | 'outline' | 'outline-filled' | 'text';
export type ButtonSize = 'small' | 'default' | 'large';
export type ButtonShape = 'default' | 'round' | 'circle';
export type ButtonStatus = 'default' | 'success' | 'warning' | 'danger';
export type ButtonTarget = '_blank' | '_self' | '_parent' | '_top';
/**
 * Button component for triggering immediate actions.
 *
 * Supports button and link modes, visual types, sizes, shapes, status
 * colors, loading state, and full-width layout.
 *
 * `aria-label`, `aria-labelledby`, and `aria-describedby` set on the host are
 * forwarded to the internal interactive control.
 *
 * @tag ve-button
 * @slot - Button label, icon, or custom content.
 * @csspart control - The internal interactive `<button>` or `<a>` element.
 * @csspart content - The wrapper around the default slot content.
 * @csspart spinner - The loading icon shown while `loading` is true.
 * @fires click - Fired when the internal control is activated while the component is available.
 */
export declare class VeButtonElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    static get observedAttributes(): string[];
    /**
     * Sets the visual type of the button.
     *
     * @attr
     */
    accessor type: ButtonType;
    /**
     * Sets the button size.
     *
     * @attr
     */
    accessor size: ButtonSize;
    /**
     * Sets the button shape.
     *
     * @attr
     */
    accessor shape: ButtonShape;
    /**
     * Sets the semantic status color of the button.
     *
     * @attr
     */
    accessor status: ButtonStatus;
    /**
     * Disables the button and prevents user interaction.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Shows a loading state and prevents user interaction.
     *
     * @attr
     */
    accessor loading: boolean;
    /**
     * Expands the button to fill the available inline size of its container.
     *
     * @attr
     */
    accessor long: boolean;
    /**
     * Renders the component as a link when set.
     *
     * @attr
     */
    accessor href: string;
    /**
     * Sets the browsing context used when `href` is provided.
     *
     * @attr
     */
    accessor target: ButtonTarget;
    private accessor controlElement;
    private accessor contentElement;
    private accessor defaultSlot;
    private accessor compact;
    private contentResizeObserver;
    private observedContentElement;
    constructor();
    private get isLinkMode();
    private get isUnavailable();
    private get controlAriaLabel();
    private get controlAriaLabelledby();
    private get controlAriaDescribedby();
    /**
     * Moves focus to the internal interactive control.
     */
    focus(options?: FocusOptions): void;
    click(): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    updated(_changedProperties: PropertyValues<this>): void;
    disconnectedCallback(): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private handleHostClick;
    private handleClick;
    private handleSlotChange;
    private observeContentElement;
    private syncCompactState;
    private shouldRenderCompactSquare;
    private hasCompactContent;
    private isCompactContentNode;
    private isCompactText;
    private renderContent;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-button': VeButtonElement;
    }
}
