import { VeElement } from '../../../foundation/base-element';
import type { StreamStatus } from '../interface';
export declare const VE_MD_TAIL_TAG = "ve-md-tail";
/**
 * Tail cursor element rendered at the end of streaming markdown to indicate
 * that more content is being generated.
 *
 * @tag ve-md-tail
 */
export declare class VeMdTailElement extends VeElement {
    static styles: import("lit").CSSResult;
    /**
     * Stream status of the surrounding markdown segment.
     *
     * @attr
     */
    accessor dataState: StreamStatus;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        [VE_MD_TAIL_TAG]: VeMdTailElement;
    }
}
