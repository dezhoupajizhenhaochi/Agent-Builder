import { VeElement } from '../../../foundation/base-element';
import { VeIconElement } from '../../ve-icon/ve-icon.element';
import type { StreamStatus } from '../interface';
export declare const VE_MD_TABLE_TAG = "ve-md-table";
/**
 * Table wrapper rendered around `<table>` segments produced by the markdown
 * parser. Provides horizontal scroll with edge fade masks, a hover toolbar
 * (copy/download), sticky headers, and `scroll-state` attribute for styling.
 *
 * @tag ve-md-table
 * @csspart scroller - 内层滚动容器。
 * @csspart fade-start - 左侧渐隐遮罩。
 * @csspart fade-end - 右侧渐隐遮罩。
 * @csspart toolbar - 悬浮工具条。
 * @csspart copy-button - 复制按钮。
 * @csspart download-button - 下载按钮。
 * @fires md-table-copy - 用户点击复制按钮后触发。
 * @fires md-table-download - 用户点击下载按钮后触发。
 */
export declare class VeMdTableElement extends VeElement {
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    static styles: import("lit").CSSResult;
    /**
     * Stream status forwarded by the parser.
     */
    accessor dataState: StreamStatus;
    /**
     * 横向滚动状态：'none' | 'start' | 'middle' | 'end'
     */
    accessor scrollState: string;
    /**
     * 是否显示工具条（复制/下载按钮）。
     */
    accessor toolbar: boolean;
    /**
     * 下载文件名（不含扩展名）。
     */
    accessor fileName: string;
    private accessor _ready;
    private accessor _scrollEl;
    private _throttledUpdate;
    private _resizeObserver;
    private _slotTable;
    private _mutationObserver;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    /** 监听 light DOM 子节点变化，将 table 移入 shadow DOM */
    private _setupMutationObserver;
    /** 将 light DOM 中的 <table> 移入 shadow DOM 的滚动容器 */
    private _adoptTable;
    private _bindObservers;
    private _updateScrollState;
    private _handleScroll;
    /** 从 shadow DOM 内的 <table> 中提取数据。 */
    private _collectData;
    private _toMarkdown;
    private _toCsv;
    private _handleCopy;
    private _handleDownload;
    private _renderToolbar;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        [VE_MD_TABLE_TAG]: VeMdTableElement;
    }
}
