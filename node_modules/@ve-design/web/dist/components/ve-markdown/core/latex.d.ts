import { type KatexOptions } from 'katex';
import type { TokenizerAndRendererExtension } from 'marked';
/**
 * 构建 KaTeX 数学公式的 marked 扩展集合，覆盖行内与块级定界符。
 * 移植自 ant-design/x 的 `packages/x-markdown/src/plugins/Latex`。
 *
 * @param katexOptions 透传给 `katex.renderToString` 的选项，默认
 *   `{ output: 'html', throwOnError: false }`。
 */
export declare function createLatexExtensions(katexOptions?: KatexOptions): TokenizerAndRendererExtension[];
