import type { UnclosedTagSet } from '../interface';
/**
 * 生成自定义子标签实例的唯一 ID，格式 `<tagName>-<instanceIndex>`。
 * Parser、Renderer 与 detect 共用此函数以保证两端匹配。
 *
 * `instanceIndex` 从 0 开始，按出现顺序计数。
 */
export declare function getTagInstanceId(tagName: string, instanceIndex: number): string;
/**
 * 跳过 `<!-- ... -->` 注释，返回结束位置（注释末尾之后），未匹配则原样返回。
 */
export declare function skipComment(html: string, pos: number): number;
/**
 * 跳过 `<![CDATA[ ... ]]>` 块，返回结束位置（CDATA 末尾之后），未匹配则原样返回。
 */
export declare function skipCDATA(html: string, pos: number): number;
/**
 * 跳过其他 `<!...>` 声明（如 DOCTYPE），返回结束位置（声明末尾之后），未匹配则原样返回。
 */
export declare function skipDeclaration(html: string, pos: number): number;
/**
 * 检测 HTML 字符串中哪些自定义子标签实例没有匹配的闭合标签。
 *
 * 返回的 Set 中元素格式为 `<tagName>-<instanceIndex>`，例如 `ve-md-mermaid-1`，
 * 其中 instanceIndex 从 0 开始，按出现顺序计数。
 */
export declare function detectUnclosedComponentTags(html: string, componentNames: readonly string[]): UnclosedTagSet;
