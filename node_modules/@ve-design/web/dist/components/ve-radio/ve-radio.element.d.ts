import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_RADIO_TAG = "ve-radio";
export declare const VE_RADIO_GROUP_TAG = "ve-radio-group";
export type VeRadioValue = string | number;
export type RadioGroupDirection = 'horizontal' | 'vertical';
export interface VeRadioChangeDetail {
    checked: boolean;
    value?: VeRadioValue;
}
export interface VeRadioGroupChangeDetail {
    value: VeRadioValue;
}
export interface VeRadioGroupOption {
    label?: unknown;
    value: VeRadioValue;
    disabled?: boolean;
}
export type RadioGroupOption = VeRadioValue | VeRadioGroupOption;
/**
 * Radio component for selecting one option from a set of mutually exclusive choices.
 *
 * Host `aria-label`, `aria-labelledby`, and `aria-describedby` attributes are
 * forwarded to the internal native radio input.
 *
 * @tag ve-radio
 * @slot - Radio label content.
 * @csspart wrapper - The internal clickable label wrapper.
 * @csspart input - Native `<input type="radio">` element.
 * @csspart indicator - Visual radio indicator.
 * @csspart dot - Checked state dot.
 * @csspart label - Label content wrapper.
 * @fires ve-change - Fired with radio checked detail when state changes.
 */
export declare class VeRadioElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Current checked state.
     *
     * @attr
     */
    accessor checked: boolean | undefined;
    /**
     * Initial checked state.
     *
     * @attr default-checked
     */
    accessor defaultChecked: boolean;
    /**
     * Whether the radio is disabled.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Submitted value of the radio.
     *
     * @attr
     */
    accessor value: VeRadioValue;
    accessor groupDisabled: boolean;
    private hasInitializedDefaultChecked;
    private get mergedDisabled();
    private get inputValue();
    connectedCallback(): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private applyDefaultChecked;
    private emitVeChangeEvent;
    private handleInputClick;
    private handleInputChange;
    render(): import("lit").TemplateResult<1>;
}
/**
 * Radio group component for managing a mutually exclusive set of radio options.
 *
 * Supports both `options`-driven rendering and custom slot children.
 *
 * @tag ve-radio-group
 * @slot - Child `ve-radio` elements.
 * @csspart group - Group wrapper container.
 * @fires ve-group-change - Fired when group selected value changes.
 */
export declare class VeRadioGroupElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-radio": typeof VeRadioElement;
    };
    static get observedAttributes(): string[];
    /**
     * Current selected value.
     *
     * @attr
     */
    accessor value: VeRadioValue;
    /**
     * Initial selected value.
     *
     * @attr default-value
     */
    accessor defaultValue: VeRadioValue;
    /**
     * Whether all radios in the group are disabled.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Layout direction of child radios.
     *
     * @attr
     */
    accessor direction: RadioGroupDirection;
    /**
     * Option list for options-mode rendering.
     *
     * @attr
     */
    accessor options: RadioGroupOption[];
    private accessor assignedRadioElements;
    private normalizedOptions;
    private hasInitializedDefaultValue;
    private readonly handleGroupChildChange;
    private get hasOptions();
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private applyDefaultValue;
    private normalizeOptions;
    private normalizeOption;
    private getSlottedRadioChildren;
    private getRadioChildren;
    private syncManagedRadio;
    private syncRadioStates;
    private isManagedRadio;
    private resolveEventRadioTarget;
    private handleSlotChange;
    private renderOptionLabel;
    private renderOptionNodes;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-radio': VeRadioElement;
        've-radio-group': VeRadioGroupElement;
    }
    interface HTMLElementEventMap {
        've-change': CustomEvent;
        've-group-change': CustomEvent;
    }
}
