/**
 * ve-citation component token contract.
 *
 * 颜色 / 圆角 / 间距全部走 global.css 中的 semantic tokens。
 */
declare const acCitationTokenDefaults: import("lit").CSSResult;
export default acCitationTokenDefaults;
