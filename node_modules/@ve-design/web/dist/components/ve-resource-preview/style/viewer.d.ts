/**
 * Styles scoped to the large preview viewer layer. Shared between
 * `ve-resource-preview` (single-item viewer) and
 * `ve-resource-preview-group` (shared viewer).
 */
declare const viewerStyles: import("lit").CSSResult;
export default viewerStyles;
