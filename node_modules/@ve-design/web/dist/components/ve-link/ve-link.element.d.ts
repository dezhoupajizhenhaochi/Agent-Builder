import { VeElement } from '../../foundation/base-element';
export declare const VE_LINK_TAG = "ve-link";
export type LinkStatus = 'primary' | 'success' | 'warning' | 'error';
export type LinkTarget = '_blank' | '_self' | '_parent' | '_top' | (string & {});
/**
 * Link component for navigation.
 *
 * Supports status colors, dotted underline, disabled state, and custom inline
 * content.
 *
 * `aria-label`, `aria-labelledby`, and `aria-describedby` set on the host are
 * forwarded to the internal interactive control.
 *
 * @tag ve-link
 * @slot - Link content, typically plain text or inline custom content.
 * @csspart control - The internal interactive `<a>` element.
 * @csspart content - The wrapper around the default content slot.
 * @fires click - Fired when the control is activated while the component is available.
 */
export declare class VeLinkElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Link destination URL.
     *
     * @attr
     */
    accessor href: string;
    /**
     * Browsing context used when `href` is provided.
     *
     * @attr
     */
    accessor target: LinkTarget;
    /**
     * Semantic status color of the link. Leave unset to use the default link color.
     *
     * @attr
     */
    accessor status: LinkStatus | undefined;
    /**
     * Disables the link and prevents user interaction.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Shows a dotted underline that follows the current status color.
     *
     * @attr
     */
    accessor underline: boolean;
    private accessor controlElement;
    private get controlAriaLabel();
    private get controlAriaLabelledby();
    private get controlAriaDescribedby();
    /**
     * Moves focus to the internal control element.
     */
    focus(options?: FocusOptions): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private handleClick;
    private renderContent;
    private renderAnchor;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-link': VeLinkElement;
    }
}
