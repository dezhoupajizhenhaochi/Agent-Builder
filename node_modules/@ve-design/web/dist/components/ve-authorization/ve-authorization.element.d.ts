import { VeElement } from '../../foundation/base-element';
export declare const VE_AUTHORIZATION_TAG = "ve-authorization";
export type AuthorizationAction = 'deny' | 'allow-once' | 'allow-always';
export type AuthorizationOrientation = 'horizontal' | 'vertical';
export interface AuthorizationActionDetail {
    action: AuthorizationAction;
}
/**
 * Authorization confirmation card for sensitive AI Agent actions.
 *
 * @tag ve-authorization
 * @slot title - Custom title content that replaces the `title` text.
 * @slot content - Custom content surface that replaces the `content` text.
 * @slot actions - Custom action controls that replace the default authorization buttons.
 * @csspart container - The outer authorization card.
 * @csspart title - The authorization title text.
 * @csspart content - The content surface.
 * @csspart actions - The action button group.
 * @csspart action - Each default action button.
 * @fires ve-authorization-action - Fired when the user selects a default authorization action.
 */
export declare class VeAuthorizationElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Authorization title text.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    private titleText;
    constructor();
    /**
     * Command, permission, or code content text.
     *
     * @attr
     */
    accessor content: string;
    /**
     * Sets the action group direction.
     *
     * @attr
     */
    accessor orientation: AuthorizationOrientation;
    /**
     * Disables default action buttons.
     *
     * @attr
     */
    accessor disabled: boolean;
    private get hasCustomContent();
    /**
     * Text for the deny action.
     *
     * @attr deny-text
     */
    accessor denyText: string;
    /**
     * Text for the allow-once action.
     *
     * @attr allow-once-text
     */
    accessor allowOnceText: string;
    /**
     * Text for the allow-always action.
     *
     * @attr allow-always-text
     */
    accessor allowAlwaysText: string;
    private getActionText;
    private emitAction;
    private handleActionClick;
    private renderContent;
    private renderAction;
    private renderActions;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-authorization': VeAuthorizationElement;
    }
    interface HTMLElementEventMap {
        've-authorization-action': CustomEvent<AuthorizationActionDetail>;
    }
}
