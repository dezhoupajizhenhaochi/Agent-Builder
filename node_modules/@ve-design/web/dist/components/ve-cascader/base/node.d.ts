import type { CascaderOption } from '../types';
import type { CascaderStoreConfig } from './store';
export interface CascaderNodeData<T extends Record<string, unknown> = CascaderOption> extends CascaderOption {
    value: string;
    label: string;
    isLeaf: boolean;
    _checked: boolean;
    _level: number;
    _index: number;
    _halfChecked: boolean;
    parent: CascaderNodeData<T> | null;
    pathValue: string[];
    children?: CascaderNodeData<T>[];
    disabled: boolean;
    disableCheckbox: boolean;
    pathLabel: string[];
}
export declare class CascaderNode<T extends Record<string, unknown> = CascaderOption> {
    value: string;
    label: string;
    disabled: boolean;
    _level: number;
    _index: number;
    isLeaf: boolean;
    disableCheckbox: boolean;
    _checked: boolean;
    _halfChecked: boolean;
    parent: CascaderNode<T> | null;
    pathValue: string[];
    pathLabel: string[];
    pathLabelText: string;
    children: CascaderNode<T>[] | undefined;
    config: CascaderStoreConfig<T>;
    _data?: CascaderNodeData<T>;
    constructor(data: T, config?: CascaderStoreConfig<T>, parent?: CascaderNode<T>);
    private initNode;
    private isHalfChecked;
    private setNodeChildrenChecked;
    getSelfChildrenValue(): string[][];
    updateHalfState(checked: boolean): void;
    setCheckedProperty(checked: boolean): void;
    setCheckedState(checked: boolean): void;
    setCheckedStateIgnoreDisabled(checked: boolean): void;
    getPathNodes(): CascaderNode<T>[];
    getChildren(): CascaderNode<T>[] | undefined;
}
