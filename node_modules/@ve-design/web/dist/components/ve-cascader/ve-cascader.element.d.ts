import { type PropertyValues, type TemplateResult } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { type CascaderNodeData } from './base/node';
import { VeIconElement } from '../ve-icon/ve-icon.element';
import type { CascaderCheckedStrategy, CascaderExpandTrigger, CascaderMode, CascaderOption, InputValueChangeReason } from './types';
export declare const VE_CASCADER_TAG = "ve-cascader";
export type VeCascaderSize = 'small' | 'default' | 'large';
export type VeCascaderStatus = 'default' | 'warning' | 'error';
export interface VeCascaderShowSearchOptions {
    panelMode?: 'cascader' | 'select';
    retainInputValue?: boolean;
    retainInputValueWhileSelect?: boolean;
    renderOption?: (inputValue: string, option: CascaderNodeData, extra: {
        checked: boolean;
    }) => unknown;
}
export interface VeCascaderChangeDetail {
    value: string[] | string[][];
    selectedOptions: CascaderOption[] | CascaderOption[][];
    dropdownVisible: boolean;
}
export interface VeCascaderOpenChangeDetail {
    open: boolean;
    reason: 'api' | 'trigger' | 'outside' | 'select' | 'escape';
}
export interface VeCascaderClearDetail {
    visible: boolean;
}
export interface VeCascaderSearchDetail {
    inputValue: string;
    reason: InputValueChangeReason;
}
/**
 * Cascader component for selecting options from a hierarchical data set.
 *
 * @tag ve-cascader
 * @slot prefix - Prefix content rendered before the selected value.
 * @csspart root - Component root container.
 * @csspart control - Trigger control.
 * @csspart prefix - Prefix slot wrapper.
 * @csspart value - Selected value container.
 * @csspart tag - Multiple selection tag.
 * @csspart tag-remove - Multiple selection tag remove button.
 * @csspart clear-button - Clear button.
 * @csspart arrow - Expand arrow.
 * @csspart popup - Popup container.
 * @csspart panel - Popup content container.
 * @csspart column - Cascader column container.
 * @csspart option - Cascader option.
 * @csspart search-option - Search result option.
 * @csspart checkbox - Multiple selection checkbox indicator.
 * @csspart footer - Custom footer container.
 * @csspart empty - Empty state container.
 * @fires ve-change - Fired when selection changes.
 * @fires ve-open-change - Fired when popup open state changes.
 * @fires ve-clear - Fired when selection is cleared.
 * @fires ve-search - Fired when search is triggered.
 * @fires ve-input-value-change - Fired when input value changes.
 */
export declare class VeCascaderElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /** Current selected value path(s). @attr */
    accessor value: string[] | string[][];
    /** Default selected value path(s). @attr default-value */
    accessor defaultValue: string[] | string[][];
    /** Cascader options. @attr */
    accessor options: CascaderOption[];
    /** Placeholder text. @attr */
    accessor placeholder: string;
    /** Cascader mode. @attr */
    accessor mode: CascaderMode;
    /** Size variant. @attr */
    accessor size: VeCascaderSize;
    /** Status variant. @attr */
    accessor status: VeCascaderStatus;
    /** Whether interactions are disabled. @attr */
    accessor disabled: boolean;
    /** Whether clear action is enabled. @attr allow-clear */
    accessor allowClear: boolean;
    /** Whether popup is open. @attr */
    accessor open: boolean;
    /** Default popup visibility. @attr default-open */
    accessor defaultOpen: boolean;
    /** Expand trigger for submenu. @attr expand-trigger */
    accessor expandTrigger: CascaderExpandTrigger;
    /** Whether each selection can trigger value change. @attr change-on-select */
    accessor changeOnSelect: boolean;
    /** Whether empty children node can expand. @attr show-empty-children */
    accessor showEmptyChildren: boolean;
    /** Strategy for multiple mode value aggregation. @attr checked-strategy */
    accessor checkedStrategy: CascaderCheckedStrategy;
    /** Enables search input. @attr show-search */
    accessor showSearch: boolean;
    /** Search input value. @attr input-value */
    accessor inputValue: string;
    /** Whether cascader panel width follows control width. Search panel always keeps at least trigger width. @attr popup-match-select-width */
    accessor popupMatchSelectWidth: boolean;
    /** Empty content fallback. @attr empty-text */
    accessor emptyText: string;
    /** Whether first search option is active by default. @attr default-active-first-option */
    accessor defaultActiveFirstOption: boolean;
    /** Search mode options. Property only. */
    accessor showSearchOptions: VeCascaderShowSearchOptions | undefined;
    /** Custom label formatter for trigger display. Property only. */
    accessor renderFormat: ((valueShow: string[], options?: CascaderOption[]) => unknown) | undefined;
    /** Custom option renderer for cascader columns. Property only. */
    accessor renderOption: ((option: CascaderNodeData, level: number) => unknown) | undefined;
    /** Custom footer renderer for each column. Property only. */
    accessor renderFooter: ((level: number, activeOption: CascaderNodeData | null) => unknown) | undefined;
    /** Custom filter method. Property only. */
    accessor filterOption: ((inputValue: string, option: CascaderNodeData) => boolean) | undefined;
    private accessor controlElement;
    private accessor panelElement;
    private accessor searchInputElement;
    private accessor normalizedValue;
    private accessor activePath;
    private accessor searchOptions;
    private accessor searchHoverIndex;
    private accessor panelPlacement;
    private store;
    private cleanupAutoUpdate;
    private repositionScheduler;
    private positionRequestId;
    private hideInputTimer;
    private initialized;
    private isSyncingValue;
    private openChangeReason;
    private stashNodeMap;
    private lastSelectedKeySetValues;
    private lastSelectedKeySet;
    private lastSearchStore;
    private lastSearchInputValue;
    private lastSearchResults;
    private getSelectedKeySet;
    private getNodeByKey;
    private readonly popupId;
    private get activeDescendantId();
    private getOptionId;
    private getBooleanAttr;
    private getOptionCheckedAttr;
    private isCheckboxDisabled;
    private isOptionSelectionDisabled;
    private isSearchOptionSelectionDisabled;
    private readonly handleDocumentPointerDown;
    private readonly handleDocumentKeydown;
    private readonly handleHostKeydown;
    private get isMultiple();
    private get mergedShowSearch();
    private get shouldShowSearchInput();
    private get shouldShowSearchPanel();
    private get shouldShowClear();
    private get mergedShowEmptyChildren();
    private get selectedPathOptions();
    /** Focuses trigger or search input if available. */
    focus(options?: FocusOptions): void;
    /** Blurs trigger and search input. */
    blur(): void;
    /** Opens popup panel. */
    show(): void;
    /** Closes popup panel. */
    hide(): void;
    addEventListener(type: 've-change', listener: ((this: VeCascaderElement, event: CustomEvent<VeCascaderChangeDetail>) => void) | null, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: 've-open-change', listener: ((this: VeCascaderElement, event: CustomEvent<VeCascaderOpenChangeDetail>) => void) | null, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: 've-clear', listener: ((this: VeCascaderElement, event: CustomEvent<VeCascaderClearDetail>) => void) | null, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: 've-search' | 've-input-value-change', listener: ((this: VeCascaderElement, event: CustomEvent<VeCascaderSearchDetail>) => void) | null, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, event: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(changedProperties: PropertyValues<this>): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    private clearHideInputTimer;
    private refreshStoreAndValue;
    private ensureSearchOptions;
    private ensureActiveNode;
    private installGlobalListeners;
    private removeGlobalListeners;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updatePosition;
    private scrollActiveOptionIntoView;
    private syncStashNodes;
    private getSelectedOptionsByValue;
    private toNodeData;
    private getSingleValueText;
    private getActiveNode;
    private setOpen;
    private emitChange;
    private emitOpenChange;
    private emitClear;
    private emitSearch;
    private shouldRetainInputValueOnHide;
    private shouldRetainInputValueWhileSelect;
    private updateInputValue;
    private commitValue;
    private handleControlClick;
    private handleControlKeydown;
    private handleSearchInput;
    private handleSearchInputClick;
    private handleSearchPanelKeydown;
    private handleCascaderPanelKeydown;
    private getMenus;
    private handleOptionClick;
    private handleMultipleChecked;
    private handleSearchOptionSelect;
    private handleClear;
    private handleRemoveTag;
    private readonly handleTagRemoveClick;
    private readonly handleFooterMouseDown;
    private readonly handlePopupInnerMouseDown;
    private readonly handleCascaderListMouseOver;
    private readonly handleCascaderListClick;
    private readonly handleCascaderListDblClick;
    private readonly handleSearchListMouseOver;
    private readonly handleSearchListMouseLeave;
    private readonly handleSearchListClick;
    private renderPrefix;
    private renderMultipleTags;
    private renderSingleValue;
    private renderValue;
    private renderActions;
    private renderTagRemoveIcon;
    private renderOptionLabel;
    private isOptionExpandable;
    private renderOptionSuffix;
    private renderOptionArrowIcon;
    private renderOptionCheckIcon;
    private renderCheckboxIndicator;
    private renderColumnList;
    private renderEmpty;
    private formatSearchLabel;
    private renderSearchOption;
    private renderSearchPanel;
    private renderCascaderPanel;
    private renderPopupContent;
    private renderPopup;
    render(): TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-cascader': VeCascaderElement;
    }
}
