import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_SPIN_TAG = "ve-spin";
export type SpinSize = 'small' | 'default' | 'large';
export type SpinDirection = 'horizontal' | 'vertical';
export interface VeSpinProps {
    size?: SpinSize;
    direction?: SpinDirection;
    autoRotate?: boolean;
    tip?: string;
}
/**
 * Spin component for lightweight loading feedback.
 *
 * @tag ve-spin
 * @slot icon - Custom loading icon that replaces the default spinner.
 * @csspart base - The outer loading container.
 * @csspart icon - The loading icon wrapper.
 * @csspart icon-graphic - The rotating loading icon graphic.
 * @csspart tip - The loading tip text.
 */
export declare class VeSpinElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Sets the loading size.
     *
     * @attr
     */
    accessor size: SpinSize;
    /**
     * Sets the layout direction between icon and tip.
     *
     * @attr
     */
    accessor direction: SpinDirection;
    /**
     * Whether to automatically rotate the loading icon.
     *
     * Accepts `auto-rotate="false"` to disable rotation in HTML.
     *
     * @attr auto-rotate
     */
    accessor autoRotate: boolean;
    /**
     * Sets the loading tip text. Use an empty string to hide the tip.
     *
     * @attr
     */
    accessor tip: string;
    private get normalizedSize();
    private get normalizedDirection();
    private get hasCustomIcon();
    private get hasTip();
    private get statusAriaLabel();
    private handleSlotChange;
    private renderDefaultIcon;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-spin': VeSpinElement;
    }
}
