import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
import { VeSelectElement } from '../ve-select/ve-select.element';
import { VeSelectItemElement } from '../ve-select/ve-select-item.element';
export declare const VE_PAGINATION_TAG = "ve-pagination";
export type PaginationChangeSource = 'page' | 'prev' | 'next';
export interface VePaginationChangeDetail {
    current: number;
    previousCurrent: number;
    pageSize: number;
    total: number;
    totalPages: number;
    source: PaginationChangeSource;
}
export interface VePaginationPageSizeChangeDetail {
    pageSize: number;
    previousPageSize: number;
    current: number;
    previousCurrent: number;
    total: number;
    totalPages: number;
}
export interface VePaginationEventMap {
    've-change': CustomEvent<VePaginationChangeDetail>;
    've-page-size-change': CustomEvent<VePaginationPageSizeChangeDetail>;
}
/**
 * Pagination component for switching between pages in large data sets.
 *
 * @tag ve-pagination
 * @csspart root - Pagination navigation container.
 * @csspart total - Total count text.
 * @csspart pages - Page item group.
 * @csspart item - Page, previous, next, and ellipsis item.
 * @csspart select - Page size select wrapper.
 * @fires ve-change - Fired when the current page changes by user interaction.
 * @fires ve-page-size-change - Fired when page size changes by user interaction.
 */
export declare class VePaginationElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
        readonly "ve-select": typeof VeSelectElement;
        readonly "ve-select-item": typeof VeSelectItemElement;
    };
    /**
     * Current page number in controlled mode.
     *
     * @attr current
     */
    accessor current: number | undefined;
    /**
     * Initial page number in uncontrolled mode.
     *
     * @attr default-current
     */
    accessor defaultCurrent: number;
    /**
     * Current page size in controlled mode.
     *
     * @attr page-size
     */
    accessor pageSize: number | undefined;
    /**
     * Initial page size in uncontrolled mode.
     *
     * @attr default-page-size
     */
    accessor defaultPageSize: number;
    /**
     * Total number of data items.
     *
     * @attr total
     */
    accessor total: number;
    /**
     * Available page size options.
     *
     * @attr page-size-options
     */
    accessor pageSizeOptions: number[];
    /**
     * Shows the total count text.
     *
     * @attr show-total
     */
    accessor showTotal: boolean;
    /**
     * Shows the page size selector.
     *
     * @attr show-page-size
     */
    accessor showPageSize: boolean;
    /**
     * Total count text template. Use `{total}` as the placeholder.
     *
     * @attr total-template
     */
    accessor totalTemplate: string;
    /**
     * Number of pages kept on each side of the current page.
     *
     * @attr buffer-size
     */
    accessor bufferSize: number;
    /**
     * Disables all pagination controls.
     *
     * @attr disabled
     */
    accessor disabled: boolean;
    private accessor currentButtonElement;
    private accessor firstEnabledButtonElement;
    private accessor innerCurrent;
    private accessor innerPageSize;
    private hasInitializedDefaults;
    private get isCurrentControlled();
    private get isPageSizeControlled();
    private get normalizedTotal();
    private get resolvedPageSize();
    private get totalPages();
    private get resolvedCurrent();
    private get normalizedPageSizeOptions();
    private get totalText();
    /**
     * Moves focus to the current page item, or the first available control.
     */
    focus(options?: FocusOptions): void;
    connectedCallback(): void;
    willUpdate(changedProperties: Map<PropertyKey, unknown>): void;
    addEventListener<K extends keyof VePaginationEventMap>(type: K, listener: (this: VePaginationElement, ev: VePaginationEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
    removeEventListener<K extends keyof VePaginationEventMap>(type: K, listener: (this: VePaginationElement, ev: VePaginationEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    private emitPageChange;
    private handlePageClick;
    private handlePreviousClick;
    private handleNextClick;
    private handlePageSizeChange;
    private renderTotal;
    private renderIconButton;
    private renderPageItem;
    private renderPages;
    private renderPageSizeSelect;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-pagination': VePaginationElement;
    }
    interface HTMLElementEventMap {
        've-change': CustomEvent;
        've-page-size-change': CustomEvent;
    }
}
