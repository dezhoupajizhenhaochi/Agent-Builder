import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_BADGE_TAG = "ve-badge";
export type BadgeStatus = 'default' | 'success' | 'warning' | 'danger' | 'processing';
/**
 * Badge component for displaying numeric labels, dots, or short text labels.
 *
 * Renders as an anchored badge when default slotted content is present, and as a
 * standalone badge when no wrapped target content is provided.
 *
 * @tag ve-badge
 * @slot - Content wrapped by the badge in anchored mode.
 * @csspart indicator - The anchored or standalone badge indicator surface.
 */
export declare class VeBadgeElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Whether to display a dot badge instead of text content.
     *
     * @attr
     */
    accessor dot: boolean;
    /**
     * Semantic status used by standalone dot badges.
     *
     * @deprecated Prefer `color` for badge color customization.
     *
     * @attr
     */
    accessor status: BadgeStatus | '';
    /**
     * Short text label shown in the badge.
     *
     * @attr
     */
    accessor text: string;
    /**
     * Custom badge color used as background color.
     *
     * @attr
     */
    accessor color: string;
    /**
     * Offset applied to the anchored badge indicator as `[x, y]` in CSS pixels.
     *
     * @attr
     */
    accessor offset: [number, number];
    private accessor hasWrappedContent;
    connectedCallback(): void;
    protected willUpdate(changedProperties: Map<string | number | symbol, unknown>): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    updated(_changedProperties: PropertyValues<this>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private syncWrappedContentState;
    private get normalizedColor();
    private syncOffsetTokens;
    private get displayText();
    private get shouldRenderIndicator();
    private get indicatorStyle();
    private renderIndicator;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-badge': VeBadgeElement;
    }
}
