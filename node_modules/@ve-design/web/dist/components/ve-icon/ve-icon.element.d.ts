import type { IconDefinition, IconName } from '../../icons/types';
import { nothing, type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_ICON_TAG = "ve-icon";
export type IconSize = number | string;
export type IconNameValue = IconName | (string & {});
/**
 * Icon renderer for VeDesign icon definitions.
 *
 * Use `name` to render a built-in icon lazily, or register a custom icon with
 * `registerIcon`.
 *
 * @tag ve-icon
 * @csspart icon - The wrapper around the registered SVG.
 */
export declare class VeIconElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Icon name resolved from the icon registry.
     *
     * @attr
     */
    accessor name: IconNameValue;
    /**
     * Inlined icon definition. When provided, the SVG is rendered directly from
     * this instance property and the global icon registry is bypassed entirely —
     * no registration or lazy loading happens. This makes the icon immune to
     * micro-frontend sandbox issues (per-realm `globalThis`, duplicated
     * registries, chunk loading failures), because the data travels with the
     * element instance instead of going through shared global state.
     *
     * Takes precedence over `name` when both are set.
     */
    accessor definition: IconDefinition | undefined;
    /**
     * Accessible label for meaningful icons. Icons are decorative by default.
     *
     * @attr
     */
    accessor label: string;
    /**
     * Sets the rendered SVG size. Numbers are treated as px values.
     *
     * @attr
     */
    accessor size: IconSize;
    private removeIconRegistryChangeListener;
    connectedCallback(): void;
    disconnectedCallback(): void;
    updated(changedProperties: PropertyValues<this>): void;
    private loadCurrentIcon;
    private get resolvedIcon();
    private get normalizedSize();
    private get iconStyle();
    render(): typeof nothing | import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-icon': VeIconElement;
    }
}
