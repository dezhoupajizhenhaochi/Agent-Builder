import { VeElement } from '../../foundation/base-element';
import { VeTooltipElement } from '../ve-tooltip/ve-tooltip.element';
export declare const VE_ARTIFACT_CARD_TAG = "ve-artifact-card";
export type ArtifactCardResolvedType = 'code' | 'word' | 'excel' | 'html' | 'image' | 'pdf' | 'zip' | 'audio' | 'video' | 'markdown' | 'ppt' | 'mind' | 'bitable' | 'chart' | 'board' | 'folder' | 'unknown';
export type ArtifactCardType = ArtifactCardResolvedType;
export interface VeArtifactCardProps {
    type?: ArtifactCardType;
    title?: string;
    src?: string;
    description?: string;
    byte?: number;
    disabled?: boolean;
}
export interface VeArtifactCardClickEventDetail {
    src: string;
    title: string;
    type: ArtifactCardResolvedType;
}
/**
 * Artifact card component for displaying generated files and media artifacts.
 *
 * The component derives file extension from `title`, displays a compact
 * metadata row, and exposes an `actions` slot for product-specific controls.
 *
 * @tag ve-artifact-card
 * @slot - Custom title content.
 * @slot actions - Custom actions rendered in the right-side action region.
 * @fires ve-card-click - Fired when the card body is activated.
 * @csspart control - The outer card container.
 * @csspart thumbnail - The thumbnail region.
 * @csspart type-icon - The default type icon.
 * @csspart text - The title and metadata region.
 * @csspart title - The title region.
 * @csspart description - The custom description or generated metadata region.
 * @csspart actions - The actions region.
 */
export declare class VeArtifactCardElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-tooltip": typeof VeTooltipElement;
    };
    /**
     * Sets the artifact type used by the default thumbnail icon and metadata label.
     *
     * @attr
     */
    accessor type: ArtifactCardType;
    /**
     * Sets the artifact title or filename. The extension is derived from this value.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    /**
     * Sets the artifact resource URI passed through click event detail.
     *
     * @attr
     */
    accessor src: string;
    /**
     * Sets custom metadata displayed below the title. When not set, the component
     * renders type and size as two separate metadata items.
     *
     * @attr
     */
    accessor description: string;
    /**
     * Sets the artifact size in bytes. The value is formatted in the default
     * metadata row.
     *
     * @attr
     */
    accessor byte: number | undefined;
    /**
     * Applies disabled presentation.
     *
     * @attr
     */
    accessor disabled: boolean;
    private accessor hasDefaultSlotContent;
    private accessor hasActionsSlotContent;
    private accessor titleOverflow;
    private accessor descriptionOverflow;
    private titleText;
    private overflowObserver;
    constructor();
    firstUpdated(): void;
    updated(): void;
    disconnectedCallback(): void;
    private measureOverflow;
    private get normalizedType();
    private get normalizedTitle();
    private get normalizedSrc();
    private get normalizedDescription();
    private get ext();
    private get typeMetaLabel();
    private renderTypeIcon;
    private renderThumbnailFace;
    private renderFolderThumbnail;
    private renderThumbnail;
    private renderDescription;
    private renderTitle;
    private renderText;
    private hasSlotContent;
    private handleTitleSlotChange;
    private handleActionsSlotChange;
    private isActionsEvent;
    private dispatchCardClick;
    private handleCardClick;
    private handleCardKeyDown;
    private renderActions;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-artifact-card': VeArtifactCardElement;
    }
    interface HTMLElementEventMap {
        've-card-click': CustomEvent<VeArtifactCardClickEventDetail>;
    }
}
