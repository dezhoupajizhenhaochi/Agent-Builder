/**
 * ve-prompt-item component token contract.
 *
 * 颜色 / 圆角 / 间距全部走 global.css 中的 semantic tokens（`--color-*` /
 * `--space-*` / `--radius-*` / `--stroke-weight-*`），此处只做 `--ve-prompt-item-*`
 * 的语义映射，避免组件内硬编码具体色值。
 */
declare const vePromptItemTokenDefaults: import("lit").CSSResult;
export default vePromptItemTokenDefaults;
