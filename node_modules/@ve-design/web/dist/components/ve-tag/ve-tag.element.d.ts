import { type CSSResultGroup } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_TAG_TAG = "ve-tag";
export type TagSize = 'small' | 'medium' | 'default' | 'large';
export type TagStatus = 'success' | 'warning' | 'error';
export interface VeTagCloseDetail {
    sourceEvent: MouseEvent;
}
/**
 * Tag component for lightweight classification, filters, and status markers.
 *
 * @tag ve-tag
 * @slot - Tag label or custom inline content.
 * @slot icon - Custom icon shown before the label. Overrides status default icons.
 * @csspart container - The tag surface.
 * @csspart control - The wrapper around the content slot.
 * @csspart icon - The icon wrapper.
 * @csspart label - The wrapper around the default slot content.
 * @csspart close - The close button shown when `closable` is true.
 * @fires ve-close - Fired when the close button is activated.
 */
export declare class VeTagElement extends VeElement {
    static styles: CSSResultGroup;
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Tag text and icon color. Any valid CSS color value is supported.
     *
     * @attr
     */
    accessor color: string;
    /**
     * Tag background color. Any valid CSS color value is supported.
     *
     * @attr bg-color
     */
    accessor bgColor: string;
    /**
     * Semantic status for status tags.
     *
     * Provides default icon, text color, and background color when set.
     *
     * @attr
     */
    accessor status: TagStatus | '';
    /**
     * Tag size.
     *
     * @attr
     */
    accessor size: TagSize;
    /**
     * Whether the close button is shown.
     *
     * @attr
     */
    accessor closable: boolean;
    /**
     * Whether the tag shows a border.
     *
     * @attr
     */
    accessor bordered: boolean;
    private accessor iconSlot;
    private accessor hasIconSlot;
    firstUpdated(): void;
    private get normalizedColor();
    private get normalizedBgColor();
    private get customColorStyle();
    private get normalizedStatus();
    private get statusIcon();
    private get shouldRenderIcon();
    private syncSlotState;
    private handleIconSlotChange;
    private emitClose;
    private handleCloseClick;
    private renderLabelContent;
    private renderIcon;
    private renderCloseButton;
    private renderControl;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-tag': VeTagElement;
    }
    interface HTMLElementEventMap {
        've-close': CustomEvent;
    }
}
