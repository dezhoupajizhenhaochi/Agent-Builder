import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_DIGITAL_INPUT_TAG = "ve-digital-input";
export type DigitalInputSize = 'small' | 'default' | 'large';
export type DigitalInputStatus = 'default' | 'error';
export type DigitalInputChangeSource = 'input' | 'blur' | 'enter' | 'step';
export interface DigitalInputChangeDetail {
    value: number;
    oldValue: number;
    source: DigitalInputChangeSource;
}
export interface VeDigitalInputEventMap {
    've-input': CustomEvent<DigitalInputChangeDetail>;
    've-change': CustomEvent<DigitalInputChangeDetail>;
}
/**
 * Digital input for entering or adjusting numeric values.
 *
 * @tag ve-digital-input
 * @csspart control - The segmented numeric control.
 * @csspart increase - Increase button.
 * @csspart input - Native input element.
 * @csspart decrease - Decrease button.
 * @csspart description - Description text below the control.
 * @fires ve-input - Fired when value changes in real time.
 * @fires ve-change - Fired when value commit happens.
 */
export declare class VeDigitalInputElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Current numeric value.
     *
     * @attr value
     */
    accessor value: number;
    /**
     * Initial numeric value in uncontrolled usage.
     *
     * @attr default-value
     */
    accessor defaultValue: number;
    /**
     * Minimum allowed value.
     *
     * @attr min
     */
    accessor min: number | undefined;
    /**
     * Maximum allowed value.
     *
     * @attr max
     */
    accessor max: number | undefined;
    /**
     * Value delta used by the increase and decrease buttons.
     *
     * @attr step
     */
    accessor step: number;
    /**
     * Number of decimal places to keep.
     *
     * @attr precision
     */
    accessor precision: number | undefined;
    /**
     * Visual size.
     *
     * @attr size
     */
    accessor size: DigitalInputSize;
    /**
     * Validation status.
     *
     * @attr status
     */
    accessor status: DigitalInputStatus;
    /**
     * Disabled state.
     *
     * @attr disabled
     */
    accessor disabled: boolean;
    /**
     * Placeholder text when the native input is empty.
     *
     * @attr placeholder
     */
    accessor placeholder: string;
    /**
     * Helper or error description.
     *
     * @attr description
     */
    accessor description: string;
    private accessor controlElement;
    private initializedDefaultValue;
    private lastCommittedValue;
    private pendingInternalValueChange;
    connectedCallback(): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    addEventListener<K extends keyof VeDigitalInputEventMap>(type: K, listener: (this: VeDigitalInputElement, ev: VeDigitalInputEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    removeEventListener<K extends keyof VeDigitalInputEventMap>(type: K, listener: (this: VeDigitalInputElement, ev: VeDigitalInputEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    /**
     * Moves focus to the internal numeric input.
     */
    focus(options?: FocusOptions): void;
    /**
     * Removes focus from the internal numeric input.
     */
    blur(): void;
    private get normalizedSize();
    private get normalizedStatus();
    private get normalizedStep();
    private get normalizedPrecision();
    private get hasMin();
    private get hasMax();
    private get canIncrease();
    private get canDecrease();
    private get displayValue();
    private normalizeValue;
    private parseInputValue;
    private setValueFromInteraction;
    private commitValue;
    private emitValueEvent;
    private handleInput;
    private handleChange;
    private handleBlur;
    private handleKeydown;
    private handleIncrease;
    private handleDecrease;
    private renderStepButton;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-digital-input': VeDigitalInputElement;
    }
}
