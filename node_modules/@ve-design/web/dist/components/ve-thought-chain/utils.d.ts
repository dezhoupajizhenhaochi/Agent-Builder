import { type TemplateResult } from 'lit';
export declare function toAriaBoolean(value: boolean): 'true' | 'false';
export declare function hasMeaningfulAssignedNodes(slot: HTMLSlotElement | null): boolean;
export declare function renderChevronIcon(): TemplateResult;
/**
 * Shared reset + focus-visible outline styles for collapse trigger buttons.
 * Used by both the root chain trigger and each item toggle.
 */
export declare const collapseTriggerResetStyles: import("lit").CSSResult;
/**
 * Shared collapse-motion styles for both the root content region and
 * each item content region.
 */
export declare const collapseMotionStyles: import("lit").CSSResult;
/**
 * Shared chevron (expand/collapse caret) styles. The parent adds a
 * `-collapsed` modifier on its wrapper to rotate the caret.
 */
export declare const chevronStyles: import("lit").CSSResult;
