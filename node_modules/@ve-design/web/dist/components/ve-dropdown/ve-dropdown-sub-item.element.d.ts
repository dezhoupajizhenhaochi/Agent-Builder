import { VeElement } from '../../foundation/base-element';
export declare const VE_DROPDOWN_SUB_ITEM_TAG = "ve-dropdown-sub-item";
/**
 * Dropdown sub item component for nested menu layers.
 *
 * @tag ve-dropdown-sub-item
 * @slot - Nested dropdown menu content.
 * @slot label - Custom submenu label content.
 * @csspart trigger - Submenu trigger row.
 * @csspart panel - Submenu panel.
 * @csspart menu - Submenu menu container.
 */
export declare class VeDropdownSubItemElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Submenu label text.
     *
     * @attr
     */
    accessor label: string;
    /**
     * Disables submenu interaction.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Controls whether the submenu panel is open.
     *
     * @attr
     */
    accessor open: boolean;
    private accessor resolvedPlacement;
    private accessor triggerElement;
    private accessor panelElement;
    private accessor menuSlot;
    private closeTimer;
    private positionRequestId;
    private cleanupAutoUpdate;
    private repositionScheduler;
    private suppressFocusOpen;
    private clearCloseTimer;
    private setOpen;
    private hasMenuContent;
    private getMenuItems;
    /**
     * Moves focus to the internal submenu trigger.
     */
    focus(options?: FocusOptions): void;
    private restoreTriggerFocus;
    private focusFirstMenuItem;
    private focusLastMenuItem;
    private focusAdjacentMenuItem;
    private scheduleUpdatePosition;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private updatePosition;
    private readonly handleMouseEnter;
    private readonly handleMouseLeave;
    private readonly handleFocusIn;
    private readonly handleFocusOut;
    private readonly handleKeydown;
    private readonly handlePanelKeydown;
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    disconnectedCallback(): void;
    private readonly handleMenuSlotChange;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-dropdown-sub-item': VeDropdownSubItemElement;
    }
}
