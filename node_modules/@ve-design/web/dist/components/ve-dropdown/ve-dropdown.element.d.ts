import { VeElement } from '../../foundation/base-element';
import { type VeFloatingPlacement } from '../../utils/floating';
import { VeDropdownItemElement, type DropdownItemSelection } from './ve-dropdown-item.element';
export declare const VE_DROPDOWN_TAG = "ve-dropdown";
export type DropdownPosition = VeFloatingPlacement;
export type DropdownTrigger = 'hover' | 'click' | 'contextmenu';
export type DropdownSelectionMode = 'none' | 'single' | 'multiple';
export type DropdownValue = string | string[] | '';
export type DropdownOpenSource = 'api' | 'trigger' | 'outside' | 'escape' | 'menu' | 'contextmenu';
export interface VeDropdownOpenChangeDetail {
    open: boolean;
    source: DropdownOpenSource;
}
export interface VeDropdownSelectDetail {
    value: string;
    label: string;
    item: VeDropdownItemElement;
    selection: DropdownItemSelection;
    selected: boolean;
    dropdownValue: DropdownValue;
}
export interface VeDropdownChangeDetail {
    value: DropdownValue;
    previousValue: DropdownValue;
    item: VeDropdownItemElement;
    selected: boolean;
}
/**
 * Dropdown component for displaying a floating operation menu.
 *
 * The `trigger` slot provides the trigger element. The default slot provides
 * strict menu content, usually composed with `ve-dropdown-item`,
 * `ve-dropdown-group`, `ve-dropdown-divider`, and `ve-dropdown-sub-item`.
 *
 * @tag ve-dropdown
 * @slot trigger - Trigger content. Use a single root node.
 * @slot - Dropdown menu content.
 * @csspart trigger - Trigger wrapper element.
 * @csspart panel - Floating panel element.
 * @csspart menu - Menu container element.
 * @fires ve-open-change - Fired when dropdown open state changes.
 * @fires ve-select - Fired when a dropdown item is activated.
 * @fires ve-change - Fired when `selection` changes the dropdown value.
 */
export declare class VeDropdownElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Sets the preferred popup placement.
     *
     * @attr
     */
    accessor position: DropdownPosition;
    /**
     * Sets the trigger mode.
     *
     * @attr
     */
    accessor trigger: DropdownTrigger;
    /**
     * Sets whether menu items are commands, single select, or multiple select.
     *
     * @attr
     */
    accessor selection: DropdownSelectionMode;
    /**
     * Current selected value. Use a string in single mode and an array in multiple mode.
     *
     * @attr
     */
    accessor value: DropdownValue;
    /**
     * Initial selected value used when `value` is empty.
     *
     * @attr default-value
     */
    accessor defaultValue: DropdownValue;
    /**
     * Controls whether the dropdown panel is open.
     *
     * @attr
     */
    accessor open: boolean | undefined;
    /**
     * Sets the initial open state for uncontrolled usage.
     *
     * @attr default-open
     */
    accessor defaultOpen: boolean;
    /**
     * Disables dropdown interactions.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Makes the dropdown stretch to full width of its container.
     *
     * @attr
     */
    accessor block: boolean;
    /**
     * Delay time for hover trigger in milliseconds.
     *
     * @attr
     */
    accessor delay: number;
    private accessor innerOpen;
    private accessor hasMenuContent;
    private accessor resolvedPlacement;
    private accessor triggerElement;
    private accessor panelElement;
    private accessor triggerSlot;
    private accessor menuSlot;
    private readonly panelId;
    private openTimer;
    private closeTimer;
    private documentBound;
    private positionRequestId;
    private contextMenuPoint;
    private cleanupAutoUpdate;
    private repositionScheduler;
    private slottedTriggerElement;
    private slottedTriggerHadDisabledAttribute;
    private suppressTriggerFocusOpen;
    private get isOpen();
    private get isControlled();
    private get canShowPanel();
    private get normalizedTrigger();
    private get normalizedPosition();
    private get canInteract();
    private get referenceElement();
    /**
     * Moves focus to the dropdown trigger.
     */
    focus(options?: FocusOptions): void;
    private restoreTriggerFocus;
    connectedCallback(): void;
    firstUpdated(): void;
    disconnectedCallback(): void;
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    private dispatchOpenChange;
    private setOpen;
    private applyDefaultValue;
    private normalizeAndSyncSelection;
    private syncSelectionState;
    private getAllDropdownItems;
    private getItemValue;
    private syncMenuContentState;
    private getSlottedTriggerElement;
    private syncSlottedTriggerDisabledState;
    private restoreSlottedTriggerDisabledState;
    private clearDelayTimers;
    private scheduleOpen;
    private scheduleClose;
    private readonly handleTriggerMouseEnter;
    private readonly handleTriggerMouseLeave;
    private readonly handleTriggerClick;
    private readonly handleTriggerContextMenu;
    private readonly handleTriggerFocusIn;
    private readonly handleTriggerFocusOut;
    private readonly handlePanelMouseEnter;
    private readonly handlePanelMouseLeave;
    private readonly handleDocumentPointerDown;
    private getMenuItems;
    private closeDescendantSubItems;
    private focusFirstMenuItem;
    private focusLastMenuItem;
    private focusAdjacentMenuItem;
    private readonly handleKeydown;
    private emitSelectionChange;
    private updateSelectionValue;
    private readonly handleMenuItemSelect;
    private syncDocumentListener;
    private removeDocumentListener;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updateContextMenuPosition;
    private updatePosition;
    private readonly handleMenuSlotChange;
    private readonly handleTriggerSlotChange;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-dropdown': VeDropdownElement;
    }
    interface HTMLElementEventMap {
        've-open-change': CustomEvent;
        've-select': CustomEvent<VeDropdownSelectDetail>;
    }
}
