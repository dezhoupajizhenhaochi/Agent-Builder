import { nothing } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_BUBBLE_DIVIDER_TAG = "ve-bubble-divider";
/**
 * 气泡分割器类型。
 * - `divider`：分割线（可带居中标签，如时间戳）
 * - `notice`：系统提示卡片（带图标 + 消息 + 可选操作按钮）
 */
export type BubbleDividerType = 'divider' | 'notice';
/**
 * 系统提示状态。
 * - `default`：中性提示（灰色）
 * - `error`：错误/超时提示（红色）
 */
export type BubbleDividerStatus = 'default' | 'error';
export interface VeBubbleDividerActionDetail {
    type: BubbleDividerType;
    status: BubbleDividerStatus;
}
/**
 * 气泡列表中的分割器组件，支持「分割线」和「系统提示」两种形态。
 *
 * @tag ve-bubble-divider
 * @csspart line - 分割线
 * @csspart label - 分割线中间的标签文字
 * @csspart body - 系统提示内容区域
 * @csspart icon - 系统提示图标容器
 * @csspart message - 系统提示消息文字
 * @csspart action - 系统提示操作按钮
 * @fires ve-bubble-divider-action - 点击操作按钮时触发
 * @slot - 系统提示消息内容（notice 模式下）
 * @slot label - 分割线标签内容（divider 模式下）
 * @slot icon - 系统提示自定义图标
 * @slot action - 系统提示自定义操作按钮内容
 */
export declare class VeBubbleDividerElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * 分割器类型。
     * @attr
     */
    accessor type: BubbleDividerType;
    /**
     * 是否显示分割线中的标签（仅 divider 模式生效）。
     * @attr
     */
    accessor showLabel: boolean;
    /**
     * 系统提示消息文本（notice 模式下的默认内容）。
     * @attr
     */
    accessor message: string;
    /**
     * 操作按钮文本（notice 模式下的默认按钮内容）。
     * @attr
     */
    accessor actionText: string;
    /**
     * 是否显示操作按钮（仅 notice 模式生效）。
     * @attr
     */
    accessor showAction: boolean;
    /**
     * 系统提示状态：default 为中性灰色，error 为红色错误态。
     * @attr
     */
    accessor status: BubbleDividerStatus;
    private accessor iconSlot;
    private accessor messageSlot;
    private accessor actionSlot;
    private accessor hasIconSlot;
    private accessor hasMessageSlot;
    private accessor hasActionSlot;
    private lightDomSlotObserver;
    private get hasActionContent();
    private get shouldRenderAction();
    connectedCallback(): void;
    firstUpdated(): void;
    disconnectedCallback(): void;
    private observeLightDomSlots;
    private syncSlotState;
    private handleSlotChange;
    private handleActionClick;
    private renderInfoIcon;
    private renderDivider;
    private renderNotice;
    render(): typeof nothing | import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        [VE_BUBBLE_DIVIDER_TAG]: VeBubbleDividerElement;
    }
}
