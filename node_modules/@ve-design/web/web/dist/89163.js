function isRenderableSlotNode(node) {
    if (node.nodeType === Node.TEXT_NODE) return (node.textContent?.trim().length ?? 0) > 0;
    return node.nodeType === Node.ELEMENT_NODE;
}
function hasAssignedSlotContent(slot) {
    const assignedNodes = slot?.assignedNodes({
        flatten: true
    }) ?? [];
    return assignedNodes.some(isRenderableSlotNode);
}
function hasNamedSlottedContent(host, slotName) {
    return Array.from(host.childNodes).some((node)=>node.nodeType === Node.ELEMENT_NODE && node.getAttribute('slot') === slotName);
}
export { hasAssignedSlotContent, hasNamedSlottedContent };
