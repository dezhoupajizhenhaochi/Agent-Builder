import { nothing } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_ALERT_TAG = "ve-alert";
export type AlertType = 'neutral' | 'info' | 'success' | 'warning' | 'error';
export interface VeAlertCloseDetail {
    sourceEvent: MouseEvent;
}
/**
 * Alert component for displaying important feedback and contextual notices.
 *
 * Keeps the API compact by exposing a default content slot and an icon slot.
 *
 * @tag ve-alert
 * @slot - Alert content.
 * @slot icon - Custom status icon. Only rendered when `show-icon` is enabled.
 * @csspart alert - The alert container.
 * @csspart icon - The status icon wrapper.
 * @csspart content - The content text wrapper.
 * @csspart close-button - The close button.
 * @fires ve-close - Fired when the close button is clicked.
 */
export declare class VeAlertElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Sets the alert status type.
     *
     * @attr
     */
    accessor type: AlertType;
    /**
     * Whether to display the status icon.
     *
     * Accepts `show-icon="false"` to hide the icon in HTML.
     *
     * @attr show-icon
     */
    accessor showIcon: boolean;
    /**
     * Whether to render a close button that dismisses the alert.
     *
     * Accepts `closable="false"` to keep the property false in HTML.
     *
     * @attr
     */
    accessor closable: boolean;
    private accessor closed;
    private accessor iconSlot;
    private accessor hasIconSlot;
    firstUpdated(): void;
    private syncSlotState;
    private syncSlottedIconSize;
    private handleSlotChange;
    private handleCloseClick;
    private renderDefaultIcon;
    private renderIcon;
    private renderCloseButton;
    render(): typeof nothing | import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-alert': VeAlertElement;
    }
    interface HTMLElementEventMap {
        've-close': CustomEvent;
    }
}
