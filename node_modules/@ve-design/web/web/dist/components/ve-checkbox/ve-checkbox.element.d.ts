import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_CHECKBOX_TAG = "ve-checkbox";
export declare const VE_CHECKBOX_GROUP_TAG = "ve-checkbox-group";
export type VeCheckboxValue = string | number;
export interface VeCheckboxChangeDetail {
    checked: boolean;
    value?: VeCheckboxValue;
}
export type CheckboxGroupDirection = 'horizontal' | 'vertical';
export interface VeCheckboxGroupOption {
    label?: unknown;
    value: VeCheckboxValue;
    disabled?: boolean;
}
export type VeCheckboxGroupOptionInput = VeCheckboxValue | VeCheckboxGroupOption;
export interface VeCheckboxGroupChangeDetail {
    value: VeCheckboxValue[];
    changedValue?: VeCheckboxValue;
    checked: boolean;
}
/**
 * Checkbox component for selecting a single option in a list of choices.
 *
 * Supports checked/indeterminate states, disabled styles,
 * controlled/uncontrolled usage, and group synchronization.
 *
 * Host `aria-label`, `aria-labelledby`, and `aria-describedby` attributes are
 * forwarded to the internal native checkbox input.
 *
 * @tag ve-checkbox
 * @slot - Checkbox label content.
 * @csspart wrapper - The internal clickable label wrapper.
 * @csspart input - Native `<input type="checkbox">` element.
 * @csspart mask - Visual checkbox mask element.
 * @csspart text - Label text wrapper.
 * @fires ve-change - Fired with checkbox checked detail when state changes.
 */
export declare class VeCheckboxElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Current checked state.
     *
     * @attr
     */
    accessor checked: boolean | undefined;
    /**
     * Initial checked state used in uncontrolled mode.
     *
     * @attr default-checked
     */
    accessor defaultChecked: boolean;
    /**
     * Whether checkbox is in indeterminate state.
     *
     * @attr
     */
    accessor indeterminate: boolean;
    /**
     * Whether checkbox is disabled.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Submitted value of the checkbox.
     *
     * @attr
     */
    accessor value: VeCheckboxValue;
    accessor groupDisabled: boolean;
    private accessor inputElement;
    private accessor defaultSlot;
    private accessor hasLabel;
    private hasInitializedDefaultChecked;
    private get mergedDisabled();
    private get inputValue();
    private get inputAriaLabel();
    private get inputAriaLabelledby();
    private get inputAriaDescribedby();
    connectedCallback(): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    updated(_changedProperties: PropertyValues<this>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private applyDefaultChecked;
    private syncNativeIndeterminateState;
    private syncLabelState;
    private emitVeChangeEvent;
    private handleInputClick;
    private handleInputEvent;
    private handleInputChange;
    private handleDefaultSlotChange;
    private renderCheckedIcon;
    render(): import("lit").TemplateResult<1>;
}
/**
 * Checkbox group component for managing a collection of checkbox values.
 *
 * Supports both `options`-driven rendering and slot children.
 *
 * @tag ve-checkbox-group
 * @slot - Child `ve-checkbox` elements.
 * @csspart group - Group wrapper container.
 * @fires ve-group-change - Fired when group selected values change.
 */
export declare class VeCheckboxGroupElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-checkbox": typeof VeCheckboxElement;
    };
    static get observedAttributes(): string[];
    /**
     * Current selected value array in controlled mode.
     */
    accessor value: VeCheckboxValue[];
    /**
     * Initial selected value array in uncontrolled mode.
     *
     * @attr default-value
     */
    accessor defaultValue: VeCheckboxValue[];
    /**
     * Whether all checkboxes in the group are disabled.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Layout direction of child checkboxes.
     *
     * @attr
     */
    accessor direction: CheckboxGroupDirection;
    /**
     * Option list for options-mode rendering.
     *
     * @attr
     */
    accessor options: VeCheckboxGroupOptionInput[];
    private normalizedOptions;
    private hasInitializedDefaultValue;
    private slottedCheckboxChildren;
    private readonly handleGroupChildChange;
    private get hasOptions();
    private get groupAriaLabel();
    private get groupAriaLabelledby();
    private get groupAriaDescribedby();
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private applyDefaultValue;
    private normalizeValueArray;
    private normalizeOptions;
    private normalizeOption;
    private collectSlottedCheckboxChildren;
    private refreshSlottedCheckboxChildren;
    private getSlottedCheckboxChildren;
    private getCheckboxChildren;
    private filterAvailableValues;
    private syncCheckboxStates;
    private isSameValueArray;
    private isManagedCheckbox;
    private handleSlotChange;
    private renderOptionLabel;
    private renderOptionNodes;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-checkbox': VeCheckboxElement;
        've-checkbox-group': VeCheckboxGroupElement;
    }
    interface HTMLElementEventMap {
        've-change': CustomEvent;
        've-group-change': CustomEvent;
    }
}
