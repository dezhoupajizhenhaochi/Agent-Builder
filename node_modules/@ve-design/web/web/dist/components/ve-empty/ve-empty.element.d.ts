import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_EMPTY_TAG = "ve-empty";
export type VeEmptyProps = Record<string, never>;
/**
 * Empty state component for displaying the absence of data or content.
 *
 * The component is presentational only. Consumers can replace the default
 * console illustration, title, description, and vetion area through slots.
 *
 * @tag ve-empty
 * @slot image - Custom illustration, image, icon, or placeholder content that replaces the default empty graphic.
 * @slot title - Custom title content. Defaults to `No Data`.
 * @slot description - Custom description content. When omitted, the default slot is used.
 * @slot actions - Action content shown below the description.
 * @slot - Backward-compatible description content.
 * @csspart base - The outer empty state container.
 * @csspart image - The illustration region.
 * @csspart body - The text content wrapper.
 * @csspart title - The title text element.
 * @csspart description - The description text element.
 * @csspart actions - The action region.
 */
export declare class VeEmptyElement extends VeElement {
    static styles: import("lit").CSSResult[];
    private accessor descriptionSlot;
    private accessor imageSlot;
    private accessor titleSlot;
    private accessor defaultSlot;
    private accessor actionsSlot;
    private accessor hasDescriptionSlot;
    private accessor hasImageSlot;
    private accessor hasTitleSlot;
    private accessor hasDefaultSlot;
    private accessor hasActionsSlot;
    private get showDescription();
    private get showBody();
    firstUpdated(_changedProperties: PropertyValues<this>): void;
    updated(_changedProperties: PropertyValues<this>): void;
    private syncSlotState;
    private handleSlotChange;
    private renderDefaultIllustration;
    private renderDescriptionFallback;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-empty': VeEmptyElement;
    }
}
