import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_BUBBLE_LIST_TAG = "ve-bubble-list";
/**
 * Vertical layout container for `ve-bubble` with optional loading indicator
 * and scroll-to-bottom pill.
 *
 * @tag ve-bubble-list
 * @slot - Any number of `ve-bubble` children.
 * @csspart indicator - The loading / scroll-to-bottom pill container.
 * @fires ve-bubble-list-scroll-bottom - Dispatched when the scroll-to-bottom pill is clicked.
 */
export declare class VeBubbleListElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Vertical gap between children. Accepts any CSS length, e.g. `'12px'` / `'1rem'`.
     * When omitted, falls back to `--ve-bubble-list-gap`.
     * @attr
     */
    accessor gap: string | null;
    /**
     * When true, shows a loading indicator (bouncing dots) at the bottom of the list.
     * @attr
     */
    accessor loading: boolean;
    /**
     * When true, enables automatic scroll-to-bottom detection.
     * A "scroll to bottom" pill appears when the user scrolls up.
     * @attr
     */
    accessor scrollIndicator: boolean;
    /**
     * Whether the list is currently scrolled away from the bottom.
     */
    private accessor _showScrollBottom;
    private _scrollContainer;
    private _resizeObserver;
    private _rafId;
    connectedCallback(): void;
    disconnectedCallback(): void;
    updated(changed: PropertyValues<this>): void;
    private _findScrollContainer;
    private _attachScrollListener;
    private _detachScrollListener;
    private _handleScroll;
    private _checkScrollPosition;
    private _handleScrollBottomClick;
    private _renderLoadingIndicator;
    private _renderScrollBottomIndicator;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-bubble-list': VeBubbleListElement;
    }
}
