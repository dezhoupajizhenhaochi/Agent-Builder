import { VeElement } from '../../foundation/base-element';
export declare const VE_BUBBLE_TAG = "ve-bubble";
export type BubblePlacement = 'start' | 'end';
export type BubbleVariant = 'filled' | 'outlined' | 'text';
export type BubbleShape = 'default' | 'round' | 'corner';
export interface VeBubbleEditConfirmDetail {
    content: string;
}
/**
 * Single chat bubble that carries one message in an AI conversation.
 *
 * @tag ve-bubble
 * @slot - Message body. Takes precedence over the `content` attribute.
 * @slot avatar - Avatar slot, recommended 32×32 round element.
 * @slot header - Top slot inside the body, e.g. role name + timestamp.
 * @slot footer - Bottom slot inside the body, e.g. feedback actions.
 * @slot quote - Custom quote label content. Falls back to the `quote-label` attribute.
 * @slot loading - Custom loading placeholder. Falls back to a built-in dot indicator.
 * @csspart root - Outer container.
 * @csspart avatar - Avatar wrapper.
 * @csspart body - Vertical container that wraps header / content / footer.
 * @csspart header - Header wrapper.
 * @csspart content - Text body wrapper, also carries the variant / shape modifiers.
 * @csspart footer - Footer wrapper.
 * @csspart loading - Loading placeholder wrapper.
 * @csspart toggle - Expand/collapse toggle button.
 * @csspart overlay - Gradient overlay in collapsed state.
 * @fires ve-bubble-edit-confirm - Dispatched when the edit confirm button is clicked.
 * @fires ve-bubble-edit-cancel - Dispatched when the edit cancel button is clicked.
 */
export declare class VeBubbleElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Bubble alignment.
     * @attr
     */
    accessor placement: BubblePlacement;
    /**
     * Visual variant.
     * @attr
     */
    accessor variant: BubbleVariant;
    /**
     * Shape variant. Has no effect when `variant="text"`.
     * @attr
     */
    accessor shape: BubbleShape;
    /**
     * Plain text shortcut for the message body. Slotted content takes precedence.
     * @attr
     */
    accessor content: string | null;
    /**
     * Show a loading placeholder instead of the message body.
     * @attr
     */
    accessor loading: boolean;
    /**
     * Maximum height before long content is automatically collapsed.
     * @attr max-height
     */
    accessor maxHeight: string;
    /**
     * Text for the "see more" button in collapsed state.
     * @attr
     */
    accessor seeMoreLabel: string;
    /**
     * Text for the "collapse" button in expanded state.
     * @attr
     */
    accessor seeLessLabel: string;
    /**
     * When true, renders a quote section at the top of the bubble content.
     * @attr
     */
    accessor hasQuote: boolean;
    /**
     * Quote label text (e.g. file name or referenced content).
     * @attr
     */
    accessor quoteLabel: string;
    /**
     * When true, the bubble enters edit mode: content is replaced by a textarea
     * with confirm/cancel buttons.
     * @attr
     */
    accessor editable: boolean;
    /**
     * Label for the confirm button in edit mode.
     * @attr
     */
    accessor editConfirmLabel: string;
    /**
     * Label for the cancel button in edit mode.
     * @attr
     */
    accessor editCancelLabel: string;
    private accessor _editValue;
    private accessor _editMinWidth;
    private accessor _expanded;
    private accessor _overflowing;
    private accessor contentEl;
    private accessor contentInnerEl;
    private accessor editInputEl;
    private accessor avatarSlot;
    private accessor headerSlot;
    private accessor footerSlot;
    private accessor defaultSlot;
    private accessor loadingSlot;
    private _resizeObserver;
    private _observedContentEl;
    private accessor hasAvatar;
    private accessor hasHeader;
    private accessor hasFooter;
    private accessor hasDefaultSlotContent;
    private accessor hasLoadingSlotContent;
    private static slotHasContent;
    private updateSlotState;
    private handleDefaultSlotChange;
    firstUpdated(): void;
    disconnectedCallback(): void;
    private requestOverflowMeasure;
    private updateContentObserver;
    private resolveMaxHeight;
    private get resolvedMaxHeight();
    private measureOverflow;
    private renderContent;
    private renderQuote;
    private renderToggle;
    private handleToggle;
    private renderLoading;
    private renderEditable;
    private _handleEditInput;
    private _handleEditConfirm;
    private _handleEditCancel;
    willUpdate(changed: Map<string, unknown>): void;
    updated(changed: Map<string, unknown>): void;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-bubble': VeBubbleElement;
    }
}
