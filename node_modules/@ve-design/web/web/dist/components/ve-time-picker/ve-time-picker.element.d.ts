import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_TIME_PICKER_TAG = "ve-time-picker";
export type TimePickerFormat = 'HH:mm:ss' | 'HH:mm';
export type TimePickerSize = 'small' | 'default' | 'large';
export type TimePickerStatus = 'default' | 'warning' | 'error';
export type TimePickerVisibleReason = 'api' | 'trigger' | 'outside' | 'select' | 'escape';
export type TimePickerChangeSource = 'select' | 'input' | 'api';
export interface VeTimePickerChangeDetail {
    value: string;
    hour: number;
    minute: number;
    second: number | undefined;
    time: TimeParts | TimeParts[];
    range: boolean;
    source: TimePickerChangeSource;
}
export interface VeTimePickerClearDetail {
    value: '';
}
export interface VeTimePickerVisibleChangeDetail {
    open: boolean;
    reason: TimePickerVisibleReason;
}
export interface VeTimePickerEventMap {
    've-change': CustomEvent<VeTimePickerChangeDetail>;
    've-clear': CustomEvent<VeTimePickerClearDetail>;
    've-visible-change': CustomEvent<VeTimePickerVisibleChangeDetail>;
}
export interface TimeParts {
    hour: number;
    minute: number;
    second: number;
}
/**
 * Time picker for selecting a specific time within a day.
 *
 * Host accessibility labels and descriptions are forwarded to the internal
 * combobox control.
 *
 * @tag ve-time-picker
 * @slot prefix - Prefix content rendered at the start of the trigger.
 * @csspart control - Trigger control.
 * @csspart label - Built-in label segment.
 * @csspart description - Help text below the trigger.
 * @csspart panel - Dropdown panel.
 * @csspart option - Time option item.
 * @csspart clear - Clear button.
 * @fires ve-change - Fired when selected time changes.
 * @fires ve-clear - Fired when current time is cleared.
 * @fires ve-visible-change - Fired when popup visibility changes.
 */
export declare class VeTimePickerElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /** Current time value. @attr */
    accessor value: string;
    /** Initial time value used only when no explicit value is provided. @attr default-value */
    accessor defaultValue: string;
    /** Placeholder text. @attr */
    accessor placeholder: string;
    /** Built-in label text rendered before the trigger. @attr */
    accessor label: string;
    /** Help text rendered below the trigger, colored by status. @attr */
    accessor description: string;
    /** Trigger width. Numbers are treated as px. @attr */
    accessor width: string;
    /** Time display and submit format. @attr */
    accessor format: TimePickerFormat;
    /** Visual size. @attr */
    accessor size: TimePickerSize;
    /** Status style. @attr */
    accessor status: TimePickerStatus;
    /** Whether interactions are disabled. @attr */
    accessor disabled: boolean;
    /** Whether a start/end time range is selected. @attr */
    accessor range: boolean;
    /** Whether loading state is shown. @attr */
    accessor loading: boolean;
    /** Whether clear action is enabled. @attr allow-clear */
    accessor allowClear: boolean;
    /** Whether popup panel is open. @attr */
    accessor open: boolean;
    /** Hour column step. @attr hour-step */
    accessor hourStep: number;
    /** Minute column step. @attr minute-step */
    accessor minuteStep: number;
    /** Second column step. @attr second-step */
    accessor secondStep: number;
    /** Minimum selectable time. @attr */
    accessor min: string;
    /** Maximum selectable time. @attr */
    accessor max: string;
    /** Whether popup width matches control width. @attr popup-match-select-width */
    accessor popupMatchSelectWidth: boolean;
    private accessor controlElement;
    private accessor inputElement;
    private accessor rangeStartElement;
    private accessor rangeEndElement;
    private accessor fieldElement;
    private accessor panelElement;
    private accessor panelPlacement;
    private accessor hasPrefixSlot;
    private accessor activeRangeEnd;
    private accessor inputValue;
    private hasInitializedDefaultValue;
    private visibilityReason;
    private repositionScheduler;
    private positionRequestId;
    private cleanupAutoUpdate;
    private listboxId;
    private descriptionId;
    constructor();
    addEventListener<K extends keyof VeTimePickerEventMap>(type: K, listener: (this: VeTimePickerElement, ev: VeTimePickerEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    removeEventListener<K extends keyof VeTimePickerEventMap>(type: K, listener: (this: VeTimePickerElement, ev: VeTimePickerEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    connectedCallback(): void;
    private readonly handleDocumentPointerDown;
    private readonly handleDocumentKeydown;
    private get normalizedFormat();
    private get shouldShowSecondColumn();
    private get normalizedValue();
    private get parsedValue();
    private get parsedRangeValue();
    private get minTimeValue();
    private get maxTimeValue();
    private get rangeStartValue();
    private get rangeEndValue();
    private get primaryValue();
    private get rangeValues();
    private getSelectedParts;
    private get displayValue();
    private get displayRangeStartValue();
    private get displayRangeEndValue();
    private get rangeStartPlaceholder();
    private get rangeEndPlaceholder();
    private get displayInputValue();
    private get shouldShowClear();
    private get hasLeadingLabel();
    private get hasDescriptionFeedback();
    private get controlAriaLabel();
    private get controlAriaLabelledby();
    private get controlAriaDescribedby();
    private get controlAriaRequired();
    private get controlAriaInvalid();
    /** Focuses the trigger. */
    focus(options?: FocusOptions): void;
    /** Blurs the trigger. */
    blur(): void;
    /** Opens the time panel. */
    show(): void;
    /** Closes the time panel. */
    hide(): void;
    firstUpdated(changedProperties: PropertyValues<this>): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    private applyDimensionProperties;
    private applyCssSizeProperty;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    disconnectedCallback(): void;
    private syncInitialAttributes;
    private applyDefaultValueIfNeeded;
    private normalizeValue;
    private normalizeSingleValue;
    private isTimePartsInRange;
    private createOptions;
    private createUnitValues;
    private isUnitValueDisabled;
    private findSelectableParts;
    private createCandidateValues;
    private selectUnitValue;
    private selectRangeUnitValue;
    private commitInputValue;
    private clearValue;
    private readonly handleClearPointerDown;
    private emitChange;
    private setOpen;
    private handleOpenChanged;
    private installGlobalListeners;
    private removeGlobalListeners;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updatePosition;
    private scrollSelectedOptionsIntoView;
    private readonly handleControlClick;
    private readonly handleControlKeydown;
    private readonly handleInputFocus;
    private readonly handleInput;
    private readonly handleInputBlur;
    private readonly handleRangeSegmentClick;
    private readonly handleLabelClick;
    private readonly handlePrefixSlotChange;
    private readonly handleClearClick;
    private createOptionClickHandler;
    private createOptionKeydownHandler;
    private renderLabel;
    private renderPrefix;
    private renderValue;
    private renderActions;
    private renderColumn;
    private renderRangePanelColumns;
    private renderPanel;
    private renderDescription;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-time-picker': VeTimePickerElement;
    }
    interface HTMLElementEventMap extends VeTimePickerEventMap {
    }
}
