import { VeElement } from '../../foundation/base-element';
export declare const VE_TAB_PANE_TAG = "ve-tab-pane";
export declare const VE_TAB_PANE_CHANGE_EVENT = "ve-tab-pane-change";
/**
 * Tab pane child item used by ve-tabs.
 *
 * @tag ve-tab-pane
 * @slot - Tab panel content.
 * @slot tab - Custom tab label content.
 */
export declare class VeTabPaneElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Unique key used to identify the pane.
     *
     * @attr
     */
    accessor key: string;
    /**
     * Fallback title text for the tab label.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    /**
     * Disables the pane tab.
     *
     * @attr
     */
    accessor disabled: boolean;
    private accessor tabSlotElements;
    private titleText;
    constructor();
    get panelId(): string;
    get tabId(): string;
    getTabNodes(): Element[];
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    private dispatchPaneChange;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-tab-pane': VeTabPaneElement;
    }
}
