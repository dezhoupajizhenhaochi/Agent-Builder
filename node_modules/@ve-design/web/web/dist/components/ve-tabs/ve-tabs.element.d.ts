import { VeElement } from '../../foundation/base-element';
import { VeTabPaneElement } from './ve-tab-pane.element';
export declare const VE_TABS_TAG = "ve-tabs";
export type TabsSize = 'small' | 'default' | 'large';
export type TabsType = 'line' | 'text' | 'rounded' | 'capsule';
export type TabsPosition = 'top' | 'right' | 'bottom' | 'left';
export interface VeTabsChangeDetail {
    activeTab: string;
}
export interface VeTabsEventMap {
    've-change': CustomEvent<VeTabsChangeDetail>;
}
/**
 * Tabs component for switching between related content panels in place.
 *
 * @tag ve-tabs
 * @slot - Accepts ve-tab-pane children.
 * @csspart nav - The tab navigation container.
 * @csspart tab - Individual tab button.
 * @csspart tab-content - The content wrapper inside each tab button.
 * @csspart panels - The panels container.
 * @csspart panel - The active tab panel wrapper.
 * @fires ve-change - Fired when the active tab changes due to user interaction.
 */
export declare class VeTabsElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-tab-pane": typeof VeTabPaneElement;
    };
    /**
     * Controls the active tab key when the component is used in controlled mode.
     * Invalid or disabled values fall back visually to the first enabled pane.
     *
     * @attr active-tab
     */
    accessor activeTab: string | undefined;
    /**
     * Sets the initial active tab key for uncontrolled usage.
     *
     * @attr default-active-tab
     */
    accessor defaultActiveTab: string;
    /**
     * Sets the overall tab size.
     *
     * @attr
     */
    accessor size: TabsSize;
    /**
     * Sets the visual style of the tab navigation.
     *
     * @attr
     */
    accessor type: TabsType;
    /**
     * Sets the placement of the tab navigation.
     *
     * @attr tab-position
     */
    accessor tabPosition: TabsPosition;
    private accessor paneElements;
    private accessor innerActiveKey;
    private accessor focusKey;
    private accessor resolvedActiveKey;
    private paneCache;
    private enabledPaneCache;
    private get panes();
    private get enabledPanes();
    private get isControlled();
    addEventListener<K extends keyof VeTabsEventMap>(type: K, listener: (this: VeTabsElement, ev: VeTabsEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
    removeEventListener<K extends keyof VeTabsEventMap>(type: K, listener: (this: VeTabsElement, ev: VeTabsEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    private handleSlotChange;
    private handlePaneChange;
    private syncFromPaneStructure;
    private refreshPaneCache;
    private warnInvalidPanes;
    private syncActiveState;
    private syncPanePresentation;
    private setActiveKey;
    private focusTabByKey;
    private moveFocus;
    private handleTabClick;
    private handleTabKeydown;
    private cloneTabNode;
    private renderTabLabel;
    private renderTabs;
    private renderPanels;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-tabs': VeTabsElement;
    }
}
