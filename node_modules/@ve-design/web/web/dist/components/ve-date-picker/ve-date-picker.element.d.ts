import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_DATE_PICKER_TAG = "ve-date-picker";
export type DatePickerFormat = string;
export type DatePickerSize = 'small' | 'default' | 'large';
export type DatePickerStatus = 'default' | 'warning' | 'error';
export type DatePickerShortcutPlacement = 'bottom' | 'left';
export type DatePickerVisibleReason = 'api' | 'trigger' | 'outside' | 'select' | 'escape';
export type DatePickerChangeSource = 'select' | 'today' | 'shortcut' | 'api';
export interface VeDatePickerChangeDetail {
    value: string;
    date: Date | Date[];
    source: DatePickerChangeSource;
}
export interface VeDatePickerClearDetail {
    value: '';
}
export interface VeDatePickerVisibleChangeDetail {
    open: boolean;
    reason: DatePickerVisibleReason;
}
export interface VeDatePickerEventMap {
    've-change': CustomEvent<VeDatePickerChangeDetail>;
    've-clear': CustomEvent<VeDatePickerClearDetail>;
    've-visible-change': CustomEvent<VeDatePickerVisibleChangeDetail>;
}
/**
 * Date picker for selecting a single calendar date.
 *
 * Host accessibility labels and descriptions are forwarded to the internal
 * combobox control.
 *
 * @tag ve-date-picker
 * @slot prefix - Prefix content rendered at the start of the trigger.
 * @csspart control - Trigger control.
 * @csspart label - Built-in label segment.
 * @csspart description - Help text below the trigger.
 * @csspart panel - Calendar popup panel.
 * @csspart cell - Calendar date cell.
 * @csspart clear - Clear button.
 * @fires ve-change - Fired when selected date changes.
 * @fires ve-clear - Fired when current date is cleared.
 * @fires ve-visible-change - Fired when popup visibility changes.
 */
export declare class VeDatePickerElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /** Current date value. @attr */
    accessor value: string;
    /** Initial date value used only when no explicit value is provided. @attr default-value */
    accessor defaultValue: string;
    /** Placeholder text. @attr */
    accessor placeholder: string;
    /** Built-in label text rendered before the trigger. @attr */
    accessor label: string;
    /** Help text rendered below the trigger, colored by status. @attr */
    accessor description: string;
    /** Date display and submit format. @attr */
    accessor format: DatePickerFormat;
    /** Visual size. @attr */
    accessor size: DatePickerSize;
    /** Status style. @attr */
    accessor status: DatePickerStatus;
    /** Whether interactions are disabled. @attr */
    accessor disabled: boolean;
    /** Whether a start/end range is selected. @attr */
    accessor range: boolean;
    /** Whether loading state is shown. @attr */
    accessor loading: boolean;
    /** Whether clear action is enabled. @attr allow-clear */
    accessor allowClear: boolean;
    /** Whether calendar panel is open. @attr */
    accessor open: boolean;
    /** Minimum selectable date. @attr */
    accessor min: string;
    /** Maximum selectable date. @attr */
    accessor max: string;
    /** Additional disabled date rules, e.g. `before:2026-01-01;dates:2026-06-18`. @attr disabled-date */
    accessor disabledDate: string;
    /** Shortcut list as JSON or `Label:value;Label:start,end`. @attr shortcuts */
    accessor shortcuts: string;
    /** Shortcut placement. @attr shortcut-placement */
    accessor shortcutPlacement: DatePickerShortcutPlacement;
    /** Default panel date before a value is selected. @attr default-picker-value */
    accessor defaultPickerValue: string;
    /** Controlled panel date. @attr picker-value */
    accessor pickerValue: string;
    /** Separator used when `range` is enabled. @attr separator */
    accessor separator: string;
    /** Whether popup width matches control width. @attr popup-match-select-width */
    accessor popupMatchSelectWidth: boolean;
    private accessor controlElement;
    private accessor fieldElement;
    private accessor panelElement;
    private accessor panelPlacement;
    private accessor hasPrefixSlot;
    private accessor viewYear;
    private accessor viewMonth;
    private accessor activeRangeEnd;
    private hasInitializedDefaultValue;
    private hasInitializedDefaultPickerValue;
    private visibilityReason;
    private repositionScheduler;
    private positionRequestId;
    private cleanupAutoUpdate;
    private gridId;
    private descriptionId;
    constructor();
    addEventListener<K extends keyof VeDatePickerEventMap>(type: K, listener: (this: VeDatePickerElement, ev: VeDatePickerEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | AddEventListenerOptions): void;
    removeEventListener<K extends keyof VeDatePickerEventMap>(type: K, listener: (this: VeDatePickerElement, ev: VeDatePickerEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => void, options?: boolean | EventListenerOptions): void;
    private readonly handleDocumentPointerDown;
    private readonly handleDocumentKeydown;
    private get normalizedFormat();
    private get normalizedValue();
    private get normalizedPickerValue();
    private get parsedValue();
    private get parsedRangeValue();
    private get rangeStartValue();
    private get rangeEndValue();
    private get primaryValue();
    private get rangeValues();
    private get displayValue();
    private get hasDisplayValue();
    private get formattedRangeStartValue();
    private get formattedRangeEndValue();
    private get minDateKey();
    private get maxDateKey();
    private get parsedShortcuts();
    private get shouldShowClear();
    private get hasLeadingLabel();
    private get hasDescriptionFeedback();
    private createResolvedShortcut;
    private normalizeShortcutValue;
    private isShortcutSelected;
    private get controlAriaLabel();
    private get controlAriaLabelledby();
    private get controlAriaDescribedby();
    private get controlAriaRequired();
    private get controlAriaInvalid();
    /** Focuses the trigger. */
    focus(options?: FocusOptions): void;
    /** Blurs the trigger. */
    blur(): void;
    /** Opens the calendar panel. */
    show(): void;
    /** Closes the calendar panel. */
    hide(): void;
    firstUpdated(changedProperties: PropertyValues<this>): void;
    willUpdate(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    disconnectedCallback(): void;
    private applyDefaultValueIfNeeded;
    private applyDefaultPickerValueIfNeeded;
    private normalizeValue;
    private normalizeSingleValue;
    private syncViewToSelectedDate;
    private syncViewToOpenDate;
    private syncViewToPickerValue;
    private getUnitKey;
    private getDisabledRuleKey;
    private isDateDisabled;
    private isDateDisabledByRules;
    private isPartsSelected;
    private isPartsInRange;
    private createDateCells;
    private selectDate;
    private selectRangeValue;
    private clearValue;
    private emitChange;
    private setOpen;
    private handleOpenChanged;
    private installGlobalListeners;
    private removeGlobalListeners;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updatePosition;
    private readonly handleControlClick;
    private readonly handleControlKeydown;
    private readonly handlePrefixSlotChange;
    private readonly handleClearClick;
    private readonly handleClearPointerDown;
    private createRangeSegmentClickHandler;
    private createCellClickHandler;
    private createCellKeydownHandler;
    private createMonthNavigationHandler;
    private createShortcutClickHandler;
    private renderLabel;
    private renderPrefix;
    private renderValue;
    private renderActions;
    private renderPanelHeader;
    private renderWeekdays;
    private renderGrid;
    private renderCalendar;
    private renderShortcuts;
    private renderPanel;
    private renderDescription;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-date-picker': VeDatePickerElement;
    }
}
