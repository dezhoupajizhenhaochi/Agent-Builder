import { VeElement } from '../../foundation/base-element';
import { type VeFloatingPlacement } from '../../utils/floating';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_POPCONFIRM_TAG = "ve-popconfirm";
export declare const VE_POPCONFIRM_OPEN_CHANGE_EVENT = "ve-open-change";
export declare const VE_POPCONFIRM_CONFIRM_EVENT = "ve-popconfirm-confirm";
export declare const VE_POPCONFIRM_CANCEL_EVENT = "ve-popconfirm-cancel";
export type PopconfirmPosition = VeFloatingPlacement;
export type PopconfirmOpenChangeSource = 'api' | 'trigger' | 'confirm' | 'cancel' | 'outside' | 'escape';
export interface PopconfirmOpenChangeDetail {
    open: boolean;
    source: PopconfirmOpenChangeSource;
}
export interface PopconfirmActionDetail {
    sourceEvent: Event;
}
/**
 * Popconfirm component for lightweight confirmation before key operations.
 *
 * The default slot provides the trigger element. The popup renders title,
 * content, icon, cancel action, and confirm action with automatic floating
 * positioning.
 *
 * @tag ve-popconfirm
 * @slot - Trigger content. Use a single root node.
 * @slot title - Custom confirmation title.
 * @slot content - Custom confirmation description.
 * @slot icon - Custom leading icon. Only rendered when `show-icon` is enabled.
 * @slot cancel - Custom content inside the built-in cancel button.
 * @slot confirm - Custom content inside the built-in confirm button.
 * @slot actions - Custom action row. Replaces the built-in cancel and confirm buttons.
 * @csspart trigger - The trigger wrapper.
 * @csspart panel - The positioned confirmation panel.
 * @csspart arrow - The optional panel arrow.
 * @csspart icon - The leading icon wrapper.
 * @csspart title - The title wrapper.
 * @csspart content - The description wrapper.
 * @csspart actions - The action button row.
 * @csspart cancel-button - The cancel action button.
 * @csspart confirm-button - The confirm action button.
 * @fires ve-open-change - Fired when the popup open state changes.
 * @fires ve-popconfirm-confirm - Fired when the confirm action is clicked.
 * @fires ve-popconfirm-cancel - Fired when the cancel action is clicked.
 */
export declare class VePopconfirmElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Sets the preferred popup placement.
     *
     * @attr
     */
    accessor position: PopconfirmPosition;
    /**
     * Confirmation title text. Use the `title` slot for rich content.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    /**
     * Confirmation description text. Use the `content` slot for rich content.
     *
     * @attr
     */
    accessor content: string;
    /**
     * Confirm action text.
     *
     * @attr ok-text
     */
    accessor okText: string;
    /**
     * Cancel action text.
     *
     * @attr cancel-text
     */
    accessor cancelText: string;
    /**
     * Controls whether the popup is open.
     *
     * @attr
     */
    accessor open: boolean | undefined;
    /**
     * Sets the initial open state for uncontrolled usage.
     *
     * @attr default-open
     */
    accessor defaultOpen: boolean;
    /**
     * Disables trigger interaction and hides the popup.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Disables the confirm action.
     *
     * @attr ok-disabled
     */
    accessor okDisabled: boolean;
    /**
     * Disables the cancel action.
     *
     * @attr cancel-disabled
     */
    accessor cancelDisabled: boolean;
    /**
     * Whether to display the leading icon.
     *
     * Accepts `show-icon="false"` to hide the icon in HTML.
     *
     * @attr show-icon
     */
    accessor showIcon: boolean;
    /**
     * Shows an arrow that points from the popup to the trigger.
     *
     * @attr
     */
    accessor arrow: boolean;
    private accessor innerOpen;
    private accessor resolvedPlacement;
    private accessor hasIconSlot;
    private accessor hasTitleSlot;
    private accessor hasContentSlot;
    private accessor hasActionsSlot;
    private accessor triggerElement;
    private accessor panelElement;
    private accessor arrowElement;
    private accessor defaultSlot;
    private accessor iconSlot;
    private accessor titleSlot;
    private accessor contentSlot;
    private readonly panelId;
    private readonly titleId;
    private readonly contentId;
    private documentBound;
    private positionRequestId;
    private cleanupAutoUpdate;
    private autoUpdateReference;
    private repositionScheduler;
    private slottedTriggerElement;
    private slottedTriggerHadDisabledAttribute;
    private titleText;
    constructor();
    private get isOpen();
    private get isControlled();
    private get canShowPanel();
    private get referenceElement();
    private get hasTitle();
    private get hasContent();
    /**
     * Opens the confirmation popup.
     */
    show(): void;
    /**
     * Closes the confirmation popup.
     */
    hide(): void;
    /**
     * Toggles the confirmation popup.
     */
    toggle(open?: boolean): void;
    /**
     * Moves focus to the trigger.
     */
    focus(options?: FocusOptions): void;
    connectedCallback(): void;
    firstUpdated(): void;
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    disconnectedCallback(): void;
    private dispatchOpenChange;
    private setOpen;
    private dispatchActionEvent;
    private getSlottedTriggerElement;
    private syncSlotState;
    private syncSlottedIconSize;
    private syncSlottedTriggerDisabledState;
    private restoreSlottedTriggerDisabledState;
    private syncDocumentListener;
    private removeDocumentListener;
    private syncOverlayState;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updatePosition;
    private readonly handleDocumentPointerDown;
    private readonly handleTriggerClick;
    private readonly handleTriggerKeydown;
    private readonly handleKeydown;
    private readonly handleConfirmClick;
    private readonly handleCancelClick;
    private readonly handleDefaultSlotChange;
    private readonly handleNamedSlotChange;
    private renderIcon;
    private renderTitle;
    private renderContent;
    private renderActions;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-popconfirm': VePopconfirmElement;
    }
    interface HTMLElementEventMap {
        've-open-change': CustomEvent;
        've-popconfirm-confirm': CustomEvent<PopconfirmActionDetail>;
        've-popconfirm-cancel': CustomEvent<PopconfirmActionDetail>;
    }
}
