import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_SIDEBAR_GROUP_TAG = "ve-sidebar-group";
export interface VeSidebarGroupToggleDetail {
    collapsed: boolean;
    label: string;
}
/**
 * Sidebar group container that hosts a list of `<ve-sidebar-item>` children
 * with an optional header label and collapsible behavior.
 *
 * @tag ve-sidebar-group
 * @slot - Default slot, accepts `<ve-sidebar-item>` children.
 * @slot label - Custom group header label content. Falls back to `label` attr.
 * @slot header-suffix - Slot for action icons/buttons on the right side of the header.
 * @csspart header - Group header (label + chevron).
 * @csspart label - Group label wrapper.
 * @csspart header-suffix - Header suffix wrapper.
 * @csspart items - Items container.
 * @fires ve-sidebar-group-toggle - Fired when collapsible group toggles.
 */
export declare class VeSidebarGroupElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Group header text.
     * @attr
     */
    accessor label: string;
    /**
     * Whether the group can be collapsed by clicking the header.
     * @attr
     */
    accessor collapsible: boolean;
    /**
     * Collapsed state of a collapsible group.
     * @attr
     */
    accessor collapsed: boolean;
    /**
     * Force showing the header even when no `label` is set; useful when
     * providing custom content via the `label` slot.
     * @attr with-label
     */
    accessor withLabel: boolean;
    private accessor labelSlot;
    private accessor hasLabelSlot;
    private lightDomSlotObserver;
    connectedCallback(): void;
    firstUpdated(): void;
    disconnectedCallback(): void;
    toggle(): void;
    private handleHeaderClick;
    private handleHeaderKeydown;
    private observeLightDomSlots;
    private syncSlotState;
    private handleSlotChange;
    private get shouldRenderHeader();
    private renderHeader;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-sidebar-group': VeSidebarGroupElement;
    }
    interface HTMLElementEventMap {
        've-sidebar-group-toggle': CustomEvent<VeSidebarGroupToggleDetail>;
    }
}
