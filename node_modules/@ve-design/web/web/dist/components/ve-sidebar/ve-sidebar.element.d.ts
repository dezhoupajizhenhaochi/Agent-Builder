import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
import { VeSidebarGroupElement } from './ve-sidebar-group.element';
import { VeSidebarItemElement } from './ve-sidebar-item.element';
export declare const VE_SIDEBAR_TAG = "ve-sidebar";
export interface VeSidebarToggleDetail {
    collapsed: boolean;
}
/**
 * Sidebar container for Agent-style workbenches. Hosts a brand area, a
 * scrollable body that contains `<ve-sidebar-group>` children (with optional
 * `slot="pinned"` sticky region) and an optional bottom footer area.
 *
 * @tag ve-sidebar
 * @slot - Default slot, accepts `<ve-sidebar-group>` children.
 * @slot logo - Brand logo (28px). Doubles as expand trigger when collapsed.
 * @slot brand - Brand text or custom node next to the logo. Falls back to the
 *   `brand` attribute when empty. Hidden in collapsed mode.
 * @slot pinned - Sticky region at the top of the body, stays visible while the
 *   rest scrolls beneath it.
 * @slot footer - Bottom user / footer area, only rendered when `with-footer`.
 * @csspart container - Outer container.
 * @csspart content - Top content wrapper (header + body).
 * @csspart header - Top header area (logo + toggle).
 * @csspart logo - Logo wrapper.
 * @csspart logo-base - Logo icon base layer (collapsed mode).
 * @csspart logo-hover - Logo hover layer showing the toggle icon.
 * @csspart logo-tooltip - Tooltip element for the collapsed logo.
 * @csspart toggle - Collapse trigger.
 * @csspart brand - Brand text.
 * @csspart body - Scrollable body container.
 * @csspart pinned - Sticky pinned region inside body.
 * @csspart footer - Bottom footer container.
 * @fires ve-sidebar-toggle - Fired when collapsed state toggles.
 */
export declare class VeSidebarElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-sidebar-group": typeof VeSidebarGroupElement;
        readonly "ve-sidebar-item": typeof VeSidebarItemElement;
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Collapsed state. When true the sidebar shrinks to icon-only width.
     * @attr
     */
    accessor collapsed: boolean;
    /**
     * Brand text rendered next to the logo when expanded.
     * @attr
     */
    accessor brand: string;
    /**
     * Whether to render the bottom footer slot. Hidden by default.
     * @attr with-footer
     */
    accessor withFooter: boolean;
    /**
     * Tooltip text shown when hovering the logo in collapsed mode.
     * @attr expand-tooltip
     */
    accessor expandTooltip: string;
    /**
     * Hide the scrollbar of the scrollable body while keeping it scrollable.
     * @attr hide-scrollbar
     */
    accessor hideScrollbar: boolean;
    private accessor _hasPinned;
    private logoTooltipEl;
    private boundLogoTrigger;
    private slotObserver;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    updated(changed: Map<PropertyKey, unknown>): void;
    toggle(): void;
    private handleToggleClick;
    private handlePinnedSlotChange;
    private handleBodyScroll;
    private propagateCollapsedState;
    private unbindLogoTrigger;
    private bindLogoTooltip;
    private positionLogoTooltip;
    private handleLogoMouseEnter;
    private handleLogoMouseLeave;
    private removeLogoListeners;
    private handleLogoScrollOrResize;
    private renderCollapsedLogo;
    private renderExpandedLogo;
    private renderToggle;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-sidebar': VeSidebarElement;
    }
    interface HTMLElementEventMap {
        've-sidebar-toggle': CustomEvent<VeSidebarToggleDetail>;
    }
}
