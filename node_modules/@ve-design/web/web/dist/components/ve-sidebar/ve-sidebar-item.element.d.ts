import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_SIDEBAR_ITEM_TAG = "ve-sidebar-item";
export type VeSidebarItemType = 'primary' | 'history';
export interface VeSidebarItemClickDetail {
    type: VeSidebarItemType;
    selected: boolean;
}
/**
 * Single sidebar menu item. Renders as a `<button>` by default, or as an
 * `<a>` when `href` is provided. Supports prefix / suffix slots, selected /
 * active / disabled / loading / unread states, optional shortcut text and a
 * tooltip shown on hover when its parent sidebar is collapsed.
 *
 * @tag ve-sidebar-item
 * @slot - Item label.
 * @slot prefix - Left icon (20px). When the parent sidebar is collapsed and
 *   `prefix` is empty, the item is hidden to avoid an empty rounded box.
 * @slot suffix - Right icon (16px).
 * @csspart control - The inner `<button>` or `<a>` control.
 * @csspart label - Item label wrapper.
 * @csspart suffix - Suffix wrapper.
 * @csspart shortcut - Shortcut text element.
 * @csspart indicator - Indicator wrapper (loading / unread).
 * @csspart tooltip - Tooltip element shown when collapsed.
 * @fires ve-sidebar-item-click - Fired when a non-disabled item is clicked.
 */
export declare class VeSidebarItemElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Visual variant of the item.
     * @attr
     */
    accessor type: VeSidebarItemType;
    /**
     * Render the item as a link when set. Falls back to `<button>` otherwise.
     * @attr
     */
    accessor href: string;
    /**
     * Anchor `target` attribute, only used when `href` is provided.
     * @attr
     */
    accessor target: string;
    /**
     * Marks the item as the currently selected route / active page.
     * @attr
     */
    accessor selected: boolean;
    /**
     * Marks the item as visually active (e.g. open dropdown trigger).
     * @attr
     */
    accessor active: boolean;
    /**
     * Disables the item.
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Shows a spinner indicator on the right side.
     * @attr
     */
    accessor loading: boolean;
    /**
     * Shows an unread dot indicator on the right side.
     * @attr
     */
    accessor unread: boolean;
    /**
     * Optional shortcut text displayed on the right side.
     * @attr
     */
    accessor shortcut: string;
    /**
     * Explicit tooltip text shown when the parent sidebar is collapsed.
     * Tooltip is only displayed when this attribute is set.
     * @attr
     */
    accessor tooltip: string;
    /**
     * Reflected by the parent `<ve-sidebar>` to indicate that the sidebar is
     * collapsed; switches the item into compact icon-only layout.
     * @attr collapsed-parent
     */
    accessor collapsedParent: boolean;
    private accessor _tooltipLabel;
    private accessor _hasPrefixIcon;
    private tooltipEl;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    private handlePrefixSlotChange;
    private syncPrefixState;
    private handleLabelSlotChange;
    /** Tooltip is only shown when the `tooltip` attribute is explicitly set. */
    private get effectiveTooltip();
    private get itemType();
    private get isLink();
    private shouldShowTooltip;
    private positionTooltip;
    private bindReposition;
    private unbindReposition;
    private handleScrollOrResize;
    private handleMouseEnter;
    private handleMouseLeave;
    private handleClick;
    private renderIndicator;
    private renderSuffix;
    private renderContent;
    private renderTooltip;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-sidebar-item': VeSidebarItemElement;
    }
    interface HTMLElementEventMap {
        've-sidebar-item-click': CustomEvent<VeSidebarItemClickDetail>;
    }
}
