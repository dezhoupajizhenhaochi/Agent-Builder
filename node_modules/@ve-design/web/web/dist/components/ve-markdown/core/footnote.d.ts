import type { MarkedExtension } from 'marked';
/**
 * 构造脚注 marked 扩展。每个 Parser 实例持有独立状态，跨 parse 调用时由
 * `preprocess` hook 重置，避免编号/定义残留。
 */
export declare function createFootnoteExtension(): MarkedExtension;
