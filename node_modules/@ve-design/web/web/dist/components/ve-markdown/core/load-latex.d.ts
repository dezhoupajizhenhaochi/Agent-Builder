/**
 * 把 KaTeX 的 `@font-face` 规则注入 document head（幂等）。
 *
 * Shadow DOM 内声明的 `@font-face` 会被浏览器忽略，因此字体必须在文档级注册；
 * 字体本身已内联为 base64 data URL，无需额外网络请求。
 */
export declare function ensureKatexFontFace(css: string): void;
/**
 * 返回可被多个 `<ve-markdown>` 实例共享的 KaTeX 布局样式表（构造一次）。
 * 供 element adopt 进 shadow root，使公式布局样式在 shadow 内生效。
 */
export declare function getKatexLayoutSheet(css: string): CSSStyleSheet | null;
