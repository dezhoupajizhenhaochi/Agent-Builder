import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
import { VeThoughtChainItemElement, type ThoughtChainLine } from './ve-thought-chain-item.element';
export declare const VE_THOUGHT_CHAIN_TAG = "ve-thought-chain";
export type { ThoughtChainLine };
export interface ThoughtChainOpenChangeDetail {
    open: boolean;
}
export interface ThoughtChainExpandDetail {
    expandedKeys: string[];
    itemKey: string;
    expanded: boolean;
}
/**
 * Thought chain component for presenting public agent execution summaries.
 *
 * It coordinates root collapse and direct child item expansion states while
 * allowing item content to contain arbitrary rich content or nested chains.
 *
 * @tag ve-thought-chain
 * @slot - Direct `ve-thought-chain-item` children.
 * @slot icon - Custom root icon.
 * @slot title - Custom root title.
 * @csspart root - The root container.
 * @csspart header - The root header area.
 * @csspart trigger - The root collapse trigger.
 * @csspart icon - The root icon wrapper.
 * @csspart title - The root title wrapper.
 * @csspart content - The chain content wrapper.
 * @csspart list - The chain list wrapper.
 * @fires ve-thought-chain-open-change - Fired when root open state is requested to change.
 * @fires ve-thought-chain-expand - Fired when a child item expansion state is requested to change.
 */
export declare class VeThoughtChainElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
        readonly "ve-thought-chain-item": typeof VeThoughtChainItemElement;
    };
    /**
     * Root title text. The `title` slot takes precedence when provided.
     *
     * @attr
     */
    get title(): string;
    set title(value: string);
    /**
     * Connector line style.
     *
     * @attr
     */
    accessor line: ThoughtChainLine;
    /**
     * Whether the root title should show a loading shimmer.
     *
     * @attr
     */
    accessor loading: boolean;
    /**
     * Whether the root chain can be collapsed.
     *
     * Accepts `collapsible="false"` to disable root collapse in HTML.
     *
     * @attr
     */
    accessor collapsible: boolean;
    /**
     * Controlled root open state.
     *
     * @attr
     */
    accessor open: boolean | undefined;
    /**
     * Initial root open state in uncontrolled mode.
     *
     * Accepts `default-open="false"` to collapse initially in HTML.
     *
     * @attr default-open
     */
    accessor defaultOpen: boolean;
    /**
     * Controlled expanded item keys. JS property only.
     */
    accessor expandedKeys: string[] | undefined;
    /**
     * Initial expanded item keys in uncontrolled mode. JS property only.
     */
    accessor defaultExpandedKeys: string[];
    private accessor itemSlot;
    private accessor titleSlot;
    private accessor iconSlot;
    private accessor hasTitleSlot;
    private accessor hasIconSlot;
    private accessor internalOpen;
    private accessor internalExpandedKeys;
    private seededDefaultExpandedItems;
    private warnedMissingKeyItems;
    private warnedDuplicateItemKeys;
    private seededInitialState;
    private readonly contentId;
    private titleText;
    constructor();
    private get actualOpen();
    private get actualExpandedKeys();
    private get hasTitleContent();
    private get shouldRenderHeader();
    willUpdate(changedProperties: PropertyValues<this>): void;
    firstUpdated(changedProperties: PropertyValues<this>): void;
    updated(changedProperties: PropertyValues<this>): void;
    private getAssignedItems;
    private syncSlotState;
    private syncChildLineOnly;
    private warnItemKeyIssues;
    private syncChildItems;
    private handleSlotChange;
    private handleRootToggle;
    private handleItemToggle;
    private renderHeaderContent;
    private renderHeader;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-thought-chain': VeThoughtChainElement;
    }
    interface HTMLElementEventMap {
        've-thought-chain-open-change': CustomEvent<ThoughtChainOpenChangeDetail>;
        've-thought-chain-expand': CustomEvent<ThoughtChainExpandDetail>;
    }
}
