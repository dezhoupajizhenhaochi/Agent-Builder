import type { CascaderOption } from '../types';
import { CascaderNode, type CascaderNodeData } from './node';
export type CascaderStoreConfig<T extends Record<string, unknown> = CascaderOption> = {
    changeOnSelect?: boolean;
    showEmptyChildren?: boolean;
    filterOption?: (inputValue: string, option: CascaderNodeData<T>) => boolean;
    showParent?: boolean;
};
export declare class CascaderStore<T extends Record<string, unknown> = CascaderOption> {
    private nodes;
    private flatNodes;
    private nodeMap;
    readonly config: CascaderStoreConfig<T>;
    constructor(options: T[], value?: string[][], config?: CascaderStoreConfig<T>);
    private calcNodes;
    private updateFlatNodes;
    setNodeCheckedByValue(initValues: string[][]): void;
    findNodeByValue(value: string[] | undefined): CascaderNode<T> | null;
    searchNodes(inputValue?: string): CascaderNode<T>[];
    getOptions(): CascaderNode<T>[];
    getNodeByKey(key: string | undefined): CascaderNode<T> | null;
    getCheckedNodes(): CascaderNode<T>[];
    getCheckedParentNodes(): CascaderNode<T>[];
}
