import { VeElement } from '../../foundation/base-element';
export declare const VE_SWITCH_TAG = "ve-switch";
export type SwitchSize = 'small' | 'default' | 'large';
export interface VeSwitchChangeDetail {
    checked: boolean;
}
/**
 * Switch component for toggling between two mutually exclusive states.
 *
 * Supports current and default values, disabled state, and three visual sizes.
 *
 * Host `aria-label`, `aria-labelledby`, and `aria-describedby` attributes are
 * forwarded to the internal button control.
 *
 * @tag ve-switch
 * @csspart control - The internal button element.
 * @csspart track - The switch track, shared with the internal button element.
 * @csspart thumb - The movable thumb.
 * @fires ve-change - Fired when the user requests a checked state change.
 */
export declare class VeSwitchElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Sets the current checked state.
     *
     * @attr
     */
    accessor checked: boolean | undefined;
    /**
     * Sets the initial checked state in uncontrolled mode.
     *
     * @attr default-checked
     */
    accessor defaultChecked: boolean;
    /**
     * Disables the switch and prevents interaction.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Sets the visual size of the switch.
     *
     * @attr
     */
    accessor size: SwitchSize;
    private accessor controlElement;
    private get controlAriaLabel();
    private get controlAriaLabelledby();
    private get controlAriaDescribedby();
    private syncControlAccessibilityAttribute;
    connectedCallback(): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    click(): void;
    private emitChangeEvents;
    private setChecked;
    private toggleChecked;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-switch': VeSwitchElement;
    }
    interface HTMLElementEventMap {
        've-change': CustomEvent;
    }
}
