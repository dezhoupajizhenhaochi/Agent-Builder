import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeButtonElement } from '../ve-button/ve-button.element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
import { type ResourcePreviewItem, type ResourcePreviewPreload, type ResourcePreviewType } from './internal';
export declare const VE_RESOURCE_PREVIEW_TAG = "ve-resource-preview";
export type { ResourcePreviewItem, ResourcePreviewPreload, ResourcePreviewType, };
export interface VeResourcePreviewPreviewDetail {
    item: ResourcePreviewItem;
    source: VeResourcePreviewElement;
    nativeEvent: Event | null;
}
export interface VeResourcePreviewDownloadDetail {
    item: ResourcePreviewItem;
    source: VeResourcePreviewElement | null;
    nativeEvent: Event | null;
}
export interface VeResourcePreviewCloseDetail {
    item: ResourcePreviewItem | null;
    source: VeResourcePreviewElement | null;
}
/**
 * Resource preview card for image and video files.
 *
 * Renders a compact preview card, lightweight inline media controls for video,
 * a download action, and a built-in large preview layer implemented with the
 * native `<dialog>` element (modal with focus trap and backdrop).
 *
 * @tag ve-resource-preview
 * @csspart root - The outer resource preview wrapper.
 * @csspart surface - The clickable media preview surface.
 * @csspart tip - The optional top-left tip badge.
 * @csspart action - The floating download action container.
 * @csspart controls - The lightweight media controls.
 * @csspart viewer - The built-in large preview layer.
 * @fires ve-preview - Fired before opening the large preview. Cancel to prevent the built-in preview.
 * @fires ve-download - Fired before the default download. Cancel to prevent default download.
 * @fires ve-preview-close - Fired when the built-in preview closes.
 */
export declare class VeResourcePreviewElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-button": typeof VeButtonElement;
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Sets the resource type.
     *
     * @attr
     */
    accessor type: ResourcePreviewType;
    /**
     * Sets the resource URL.
     *
     * @attr
     */
    accessor src: string;
    /**
     * Sets the display name used by labels, preview title, and download fallback.
     *
     * @attr
     */
    accessor name: string;
    /**
     * Sets the optional tip badge text shown over the media.
     *
     * @attr
     */
    accessor tip: string;
    /**
     * Sets the video poster URL. Only applies to `type="video"`.
     *
     * @attr
     */
    accessor poster: string;
    /**
     * Sets media preload behavior for video.
     *
     * @attr
     */
    accessor preload: ResourcePreviewPreload;
    /**
     * Disables the built-in preview opening interaction.
     *
     * @attr
     */
    accessor previewDisabled: boolean;
    /**
     * Hides the built-in download action. Standard HTML boolean attribute semantics:
     * present = true (hide), absent = false (show).
     *
     * @attr
     */
    accessor hideDownload: boolean;
    private accessor inlineMediaElement;
    private accessor viewerMediaElement;
    private accessor viewerDialog;
    private accessor previewVisible;
    private accessor playing;
    private accessor muted;
    private accessor currentTime;
    private accessor duration;
    private accessor mediaAspectRatio;
    private accessor viewerScale;
    private get normalizedType();
    private get normalizedPreload();
    private get hasSource();
    updated(changedProperties: PropertyValues<this>): void;
    disconnectedCallback(): void;
    /**
     * Returns the normalized resource descriptor.
     */
    getResourceItem(): ResourcePreviewItem;
    /**
     * Opens the built-in preview layer.
     */
    openPreview(): void;
    /**
     * Closes the built-in preview layer.
     */
    closePreview(): void;
    private getMediaElement;
    private syncMediaState;
    private handleImageLoad;
    private handleMediaEvent;
    private handleSurfaceClick;
    private handleKeyboardPreview;
    private handleDialogClose;
    private handleDialogCancel;
    private handleZoom;
    private get viewerScaleStyle();
    private requestPreview;
    private pauseMedia;
    private toggleMedia;
    private toggleMuted;
    private seekMedia;
    private emitDownload;
    private renderCardMedia;
    private renderTip;
    private renderDownloadButton;
    private renderViewer;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-resource-preview': VeResourcePreviewElement;
    }
    interface HTMLElementEventMap {
        've-preview': CustomEvent<VeResourcePreviewPreviewDetail>;
        've-preview-close': CustomEvent<VeResourcePreviewCloseDetail>;
    }
}
