import { type TemplateResult } from 'lit';
import { VeElement } from '../../foundation/base-element';
export declare const VE_CHAT_INPUT_ACTION_TAG = "ve-chat-input-action";
export type ChatInputActionType = 'button' | 'dropdown';
export type ChatInputActionStatus = 'default' | 'active';
export type ChatInputActionSize = 'small' | 'default' | 'large';
export interface ChatInputActionClickDetail {
    type: ChatInputActionType;
    open: boolean;
}
/**
 * `<ve-chat-input-action>` is a rounded toggle/dropdown button used inside
 * `<ve-chat-input>`'s left action area. Supports `button` and `dropdown`
 * shapes plus `default` / `active` visual states.
 *
 * @tag ve-chat-input-action
 * @slot - Button label content.
 * @slot icon - Leading icon displayed before the label.
 * @csspart button - The inner button element.
 * @csspart icon - The leading icon wrapper.
 * @csspart label - The default slot wrapper.
 * @csspart arrow - The trailing arrow (dropdown only).
 * @fires ve-chat-input-action-click - Fired when the button is clicked
 *        (dropdown clicks also fire this; use to react to user intent).
 */
export declare class VeChatInputActionElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /** Action shape. `dropdown` shows a trailing arrow and emits a click event. */
    accessor type: ChatInputActionType;
    /** Visual state. `active` paints the secondary surface and removes the border. */
    accessor status: ChatInputActionStatus;
    /** Size variant; usually inherited from the parent `<ve-chat-input>`. */
    accessor size: ChatInputActionSize;
    /** Disable interactions. */
    accessor disabled: boolean;
    /** Reflects the open state of an associated dropdown so parent can rotate the arrow. */
    accessor open: boolean;
    /** Render as an icon-only rounded-square button with no text padding. */
    accessor iconOnly: boolean;
    private accessor hasIconSlot;
    private handleIconSlotChange;
    private handleClick;
    private renderArrow;
    render(): TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-chat-input-action': VeChatInputActionElement;
    }
    interface HTMLElementEventMap {
        've-chat-input-action-click': CustomEvent<ChatInputActionClickDetail>;
    }
}
