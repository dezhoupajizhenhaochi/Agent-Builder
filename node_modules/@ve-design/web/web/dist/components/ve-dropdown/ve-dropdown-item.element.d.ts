import { VeElement } from '../../foundation/base-element';
import { VeIconElement } from '../ve-icon/ve-icon.element';
export declare const VE_DROPDOWN_ITEM_TAG = "ve-dropdown-item";
export declare const VE_DROPDOWN_ITEM_SELECT_EVENT = "ve-dropdown-item-select";
export type DropdownItemSelection = 'none' | 'check' | 'checkbox' | 'switch';
export interface VeDropdownItemSelectDetail {
    value: string;
    label: string;
    item: VeDropdownItemElement;
    selection: DropdownItemSelection;
    selected: boolean;
}
/**
 * Dropdown menu item component.
 *
 * @tag ve-dropdown-item
 * @slot - Menu item content.
 * @csspart button - Inner menu button.
 * @csspart checkbox - Multiple-selection checkbox indicator.
 * @csspart switch - Switch-selection indicator.
 * @fires ve-dropdown-item-select - Fired when the menu item is activated.
 */
export declare class VeDropdownItemElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-icon": typeof VeIconElement;
    };
    /**
     * Value emitted when this item is selected.
     *
     * @attr
     */
    accessor value: string;
    /**
     * Disables this menu item.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Marks this item as a danger action.
     *
     * @attr
     */
    accessor danger: boolean;
    /**
     * Marks this item as selected.
     *
     * @attr
     */
    accessor selected: boolean;
    /**
     * Sets the selection indicator rendered for this item.
     *
     * @attr
     */
    accessor selection: DropdownItemSelection;
    private accessor buttonElement;
    private get textLabel();
    private get roleType();
    private get isSelectable();
    /**
     * Moves focus to the internal menu button.
     */
    focus(options?: FocusOptions): void;
    private commitSelectionState;
    private select;
    private readonly handleClick;
    private readonly handleKeydown;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-dropdown-item': VeDropdownItemElement;
    }
    interface HTMLElementEventMap {
        've-dropdown-item-select': CustomEvent<VeDropdownItemSelectDetail>;
    }
}
