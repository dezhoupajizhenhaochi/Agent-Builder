import { VeElement } from '../../foundation/base-element';
import { type VeFloatingPlacement } from '../../utils/floating';
export declare const VE_TOOLTIP_TAG = "ve-tooltip";
export type TooltipPosition = VeFloatingPlacement;
export type TooltipTrigger = 'hover' | 'click' | 'focus';
export interface TooltipOpenChangeDetail {
    open: boolean;
}
/**
 * Tooltip component for showing lightweight, non-interactive supplemental information near a trigger.
 *
 * The default slot provides the trigger element. The `content` named slot provides
 * the floating tooltip content.
 *
 * @tag ve-tooltip
 * @slot - Trigger content. Use a single root node.
 * @slot content - Tooltip popup content.
 * @csspart trigger - The trigger wrapper.
 * @csspart panel - The positioned tooltip panel.
 * @csspart content - The popup content wrapper.
 * @csspart arrow - The tooltip arrow element.
 * @fires ve-open-change - Fired when the tooltip open state changes.
 */
export declare class VeTooltipElement extends VeElement {
    static styles: import("lit").CSSResult[];
    /**
     * Sets the preferred popup placement.
     *
     * @attr
     */
    accessor position: TooltipPosition;
    /**
     * Sets the trigger mode.
     *
     * @attr
     */
    accessor trigger: TooltipTrigger;
    /**
     * Shows an arrow that points from the tooltip panel to the trigger.
     *
     * @attr
     */
    accessor arrow: boolean;
    /**
     * Controls whether the tooltip is open.
     *
     * @attr
     */
    accessor open: boolean | undefined;
    /**
     * Sets the initial open state for uncontrolled usage.
     *
     * @attr default-open
     */
    accessor defaultOpen: boolean;
    /**
     * Disables tooltip interactions and popup visibility.
     *
     * @attr
     */
    accessor disabled: boolean;
    /**
     * Sets the hide delay in milliseconds.
     *
     * @attr
     */
    accessor delay: number;
    private accessor innerOpen;
    private accessor hasContent;
    private accessor resolvedPlacement;
    private accessor triggerElement;
    private accessor panelElement;
    private accessor arrowElement;
    private accessor defaultSlot;
    private accessor contentSlot;
    private readonly panelId;
    private openTimer;
    private closeTimer;
    private repositionScheduler;
    private cleanupAutoUpdate;
    private autoUpdateReference;
    private documentBound;
    private positionRequestId;
    private describedElement;
    private get referenceElement();
    private get isOpen();
    private get isControlled();
    private get canShowPanel();
    private get isInteractiveTrigger();
    connectedCallback(): void;
    firstUpdated(): void;
    disconnectedCallback(): void;
    updated(changedProperties: Map<PropertyKey, unknown>): void;
    private dispatchOpenChange;
    private setOpen;
    private syncContentState;
    private getSlottedTriggerElement;
    private clearDelayTimers;
    private scheduleOpen;
    private scheduleClose;
    private readonly handleTriggerMouseEnter;
    private readonly handleTriggerMouseLeave;
    private readonly handleTriggerClick;
    private readonly handleTriggerFocusIn;
    private readonly handleTriggerFocusOut;
    private readonly handlePanelMouseEnter;
    private readonly handlePanelMouseLeave;
    private readonly handleKeydown;
    private readonly handleDocumentPointerDown;
    private syncDocumentListener;
    private syncTriggerDescription;
    private clearTriggerDescription;
    private addDescribedByToken;
    private removeDescribedByToken;
    private syncOverlayState;
    private removeDocumentListener;
    private setupAutoUpdate;
    private teardownAutoUpdate;
    private scheduleUpdatePosition;
    private updatePosition;
    private readonly handleContentSlotChange;
    private readonly handleDefaultSlotChange;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-tooltip': VeTooltipElement;
    }
    interface HTMLElementEventMap {
        've-open-change': CustomEvent;
    }
}
