import { VeElement } from '../../foundation/base-element';
export declare const VE_AVATAR_TAG = "ve-avatar";
/**
 * Avatar component for displaying a user, team, or entity identity.
 *
 * Supports slotted text, image, icon, and custom content.
 *
 * @tag ve-avatar
 * @slot - Custom fallback avatar content.
 * @csspart control - The main avatar control surface.
 * @csspart content - The slotted avatar content container.
 */
export declare class VeAvatarElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static get observedAttributes(): string[];
    /**
     * Avatar size. Pure numbers are treated as CSS pixels.
     *
     * @attr
     */
    accessor size: string | number;
    private get hasExplicitSize();
    private get normalizedSize();
    connectedCallback(): void;
    protected willUpdate(changedProperties: Map<string | number | symbol, unknown>): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    private syncHostSizeToken;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-avatar': VeAvatarElement;
    }
}
