import { type PropertyValues } from 'lit';
import { VeElement } from '../../foundation/base-element';
import { VeAvatarElement } from './ve-avatar.element';
export declare const VE_AVATAR_GROUP_TAG = "ve-avatar-group";
/**
 * Avatar group component for arranging multiple avatars with overlap.
 *
 * @tag ve-avatar-group
 * @slot - Slot that accepts multiple `ve-avatar` elements.
 * @csspart group - The avatar group wrapper.
 * @csspart summary - The collapsed avatar count summary.
 */
export declare class VeAvatarGroupElement extends VeElement {
    static styles: import("lit").CSSResult[];
    static readonly dependencies: {
        readonly "ve-avatar": typeof VeAvatarElement;
    };
    static get observedAttributes(): string[];
    /**
     * Shared size for child avatars. Pure numbers are treated as CSS pixels.
     * Individual avatars can override it.
     *
     * @attr
     */
    accessor size: string | number;
    /**
     * Maximum number of visible avatars before collapsing the rest into `+x`.
     *
     * @attr
     */
    accessor maxCount: number;
    private accessor avatars;
    private get hasExplicitSize();
    private get normalizedSize();
    private get normalizedOverlap();
    protected willUpdate(changedProperties: Map<string | number | symbol, unknown>): void;
    connectedCallback(): void;
    attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void;
    updated(_changedProperties: PropertyValues<this>): void;
    private handleSlotChange;
    private syncGroupSizeToken;
    private syncGroupOverlapToken;
    private resolveOverlapAvatarSize;
    private syncChildAvatarSizeTokens;
    private syncAvatarZIndex;
    private getVisibleAvatars;
    firstUpdated(): void;
    render(): import("lit").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        've-avatar-group': VeAvatarGroupElement;
    }
}
