/**
 * Component-scoped normalize rules.
 *
 * Based on normalize.css v8.0.1:
 * MIT License | github.com/necolas/normalize.css
 *
 * The rules are adapted for Shadow DOM. Global document targets such as
 * `html` and `body` are intentionally mapped to `:host` or omitted.
 */
export declare const componentReset: import("lit").CSSResult;
